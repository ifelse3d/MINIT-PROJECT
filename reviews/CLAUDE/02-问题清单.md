# 02 · 问题清单 — Claude（2026-08-24）

> 按严重度排。标 **[新]** 的是 PROMPT 第 5～9 节没写、我自己发现的；
> 其余是我对已列问题的判断（同意就一句话，不重述）。
> 每条「为什么会变成这样」里标明哪些是推测。

---

### [C-01] 核心卖点「读手写」至今零证据，而这是 7 天内修得掉的
- **严重度**：🔴 挡住上线（也挡住比赛）
- **在哪**：`eval/real/` 不存在（实测）；`eval/cases/*/input.png` 10 张全印刷体（开过 case-01 亲眼看）；`competition/evidence-tracker.md` 第 1 格 ☐
- **谁会踩到**：评审（最伤问题）／之后每一个真实使用者
- **会发生什么**：评审一句「给我看一张真手写」就终局；上线后第一张真手写的表现是未知数
- **为什么会变成这样**：拍照＋写标准答案只有 J 能做（`17-要你做的事` §2 从 8/23 挂到今天）。不是工程问题，是「卡在唯一的人身上」的问题

### [C-02] [新] 今天实测 invented = 1：「AI 不准编」的承诺在现行模型上破了
- **严重度**：🔴 挡住上线（这是写进 CLAUDE.md 硬规矩 1、印上 deck 的承诺）
- **在哪**：`eval/reports/eval-2026-08-24T10-21-05.md`——case-04-minutes-mixed 多生出一条决议
  「Kutipan derma bulan Jun: RM830.00」（把笔记里的数字自行升格成决议）。总分 **92.9%（117/126），不是 95.2%**
- **谁会踩到**：都会。评审若核对 deck 上「zero invented」（8/9 的旧数字）与现状，就是诚信问题
- **会发生什么**：一条没人做过的决议进了正式会议记录，输出里毫无迹象——正是 8/19 那次「17 条少 5 条」的镜像
- **为什么会变成这样**：8/4 预设模型换成 flash-lite 后没有人重跑 eval（STATE.md §6 自己预言过「换模型让量测作废」）；我这次只是把它跑了

### [C-03] [新] flash-lite 会截断电话号码——WhatsApp 收据会发去错的号码
- **严重度**：🔴 会流失使用者（钱和信任一起丢）
- **在哪**：同一份今日报告：`rows[0].donor_phone` 期望 `012-3456789` 读成 `012-345678`；`rows[2]` 期望 `03-98765432` 读成 **`03-9`**
- **谁会踩到**：财政（发收据那一刻）、捐款人（收不到）
- **会发生什么**：`buildWaMeLink`（`register-receipts.tsx:311`）拿着截断的号码生成链接，一按就发给错误号码或发不出去；而电话栏位没有长度／格式校验兜底
- **为什么会变成这样**：推测——电话是「text」型欄位，没有像金额（100%）那样有确定性校验；prompt 也没有「号码宁缺勿短、逐字抄」的规则

### [C-04] migration 16、17 没跑——行事历／现金交接／死线打勾是单机玩具
- **严重度**：🔴 挡住上线；**修复成本 10 分钟**，全案投报率最高
- **在哪**：`npm run check:migrations` 今日实测 NOT YET × 2；工具已修（`756fb09`）
- **判断**：同意 PROMPT 5.3 的全部描述。降级处理做得好（存不进就退回本机＋画面明说，`STATE.md` §6），所以不是灾难——但「三家评审都在看、而 J 十分钟能解」的事拖着，本身就是问题

### [C-05] 无草稿、历史不可回改——真实秘书第一场会议就会流失
- **严重度**：🔴 会流失使用者
- **在哪**：盘点 9、10；`minutes-store.tsx` 其实有 localStorage 还原（`:92`），缺的是「确认之后还能拿回来改」和跨设备
- **判断**：同意。补充：localStorage 还原意味着「中途走开」在**同一台设备**上其实活得下来——真正断头的是「确认后发现错字」和「换设备」。修的时候先修这两个，比重做草稿系统便宜得多

### [C-06] 收据开错没有作废／重开的路
- **严重度**：🔴 会流失使用者（表 C 的「不敢按」之首）
- **在哪**：§9.6；`register-receipts.tsx:357` 注解只解释了为什么不能删
- **判断**：同意，且「连号防弊」的设计是对的——所以答案不是允许删，是 **void＋重开**（作废票留在序列里、印 BATAL，新号重发）。会计惯例现成，不必发明

### [C-07] [新] 商业数字的病根：今天的模型分流不在任何已定价情境里
- **严重度**：🟠 会在评审 Q&A 里爆
- **在哪**：`npm run check:ai` 今日输出原话：「This combination is not one of the priced scenarios in src/lib/unit-economics.ts — do not quote a margin for it.」现行分流是 classify=gpt-5-nano／extract=flash-lite／chat=gpt-5.6-luna
- **谁会踩到**：评审问毛利时，任何一个已公布数字（73.4%／83%／RM19.56）都对不上现在真正跑的组合
- **判断**：PROMPT 5.8 说「数字自己跟自己打架」——同意，但病根比矛盾更深一层：**连「现在这套」都没被定价**。先加情境、再删旧数字，顺序不能反

### [C-08] /minutes 步骤编号两套对不上（按 2 进去看到 3）
- **严重度**：🟠 会流失使用者（照编号找路的正是 B/C 人群）
- **在哪**：`section-tabs.tsx:83` 自动 `{i+1}` vs 写死的 `notes-review.tsx:67 step={1}`、`:255 step={2}`、`attendance-review.tsx:145 step={3}`、`minutes-document.tsx:94 step={4}`、`:325 step={5}`（全部亲验）；`/money` 侧对得上
- **判断**：同意 7.6。修法半天内：把区块编号改由 layout 供值，或干脆拿掉区块编号只留分页编号

### [C-09] 三语串接＋侧栏 truncate——预设状态是三个人群各看两份自己不要的字
- **严重度**：🟠
- **在哪**：`language-provider.tsx:16`（预设全开）、`:83`（`join(" · ")`）、`sidebar.tsx:92,138`（truncate）
- **判断**：同意 7.2。最便宜的修法不是重做 i18n：**首次登入问一句「您想看哪（几）种文字？」**，把右上角那三颗按钮的能力放到人第一眼看得到的地方。侧栏内再让 `<Tri>` 只显示第一顺位语言

### [C-10] [新] `r1-draft/` 里躺着一份「另一个产品」的 deck，名字比正版更像正版
- **严重度**：🟠 交件事故一次就致命
- **在哪**：`competition/r1-draft/Minit-R1-Competition-Deck.pdf`（712KB）＋ `05_DECK_10_SLIDE_CONTENT.md`——内容是「Community Request-to-Resolution」（修走道灯的报修工作流），每页盖 `FICTIONAL DATA`，与现在的产品毫无关系
- **会发生什么**：赶截止那晚随手抓「Competition-Deck」交上去＝交了一个不存在的产品。这正是 STATE.md §6 记录过的「旧档案占着干净的名字」陷阱原样重演
- **怎么防**：整个 `r1-draft/`、`r1c-draft/` 移进 `competition/archive/`，根目录只留 `deck/Minit-Pitch-Deck-CURRENT.*`

### [C-11] function calling：OpenAI 路径今天已实测通过，Gemini 路径仍未验证
- **严重度**：🟠 → 对现况其实是 🟡（详见判断）
- **在哪**：`npm run eval:tools` 今日 **18/18 全对**（openai:gpt-5.6-luna，真 API）；`tool-wire.ts` 的 Gemini 分支仍然没被真呼叫过
- **判断**：**不同意 PROMPT 5.5 的严重度**。它写「第一次真的呼叫会不会 400，现在没人知道」——现在知道了：现行分流的 chat 走 OpenAI，而 OpenAI 路径全通。真正剩的风险是「哪天把 chat 切回 Gemini」那一下，压成一条待办即可

### [C-12] [新] 安全边界比 PROMPT 给人的印象好——这是「比它自己以为的好」的一条
- **严重度**：🟢（正面发现）
- **在哪**：`src/proxy.ts:87`（未登入打 `/api/*` 回 401 JSON，不是只有页面跳转）；`active-org.ts:3`（cookie 不是安全边界、RLS 才是，写成注释且实现一致）；`api/intake/route.ts:123`（**先扣额度再呼叫厂商**）、`:105`（页数上限在扣费前）；`usage.ts:178`（计费失败 fail-closed）
- **判断**：三家外部评审若只读 PROMPT 第 5～6 节，会把这个专案想象成一个到处漏的原型。抽查结果相反：钱和权限的边界是认真做的。评分时我把这个算进了 Technical Feasibility 的底分

### [C-13] agm-pack 全页示范＋`committee_roster` 没接进任何交出去的文件
- **严重度**：🟡（因为有 CONTOH 水印兜底）
- **判断**：同意 §8.17 与 STATE.md 的分析（「挡一份盖着请勿使用的示范档不是合规，是表演」）。7 天内不要动它——接线是 eROSES mapping 级别的工程，见 03 的「砍哪个」

### [C-14] 六棵树＋57 份文档的腐坏成本是这个专案最大的慢性病
- **严重度**：🟡（不挡 7 天，但每周都在收利息）
- **判断**：同意 5.9。第 6 节那 20 几条错误里，至少 6 条的根源是「两份东西要人手同步」（bat＋探针、文件 vs 程式、两棵树）。**规矩已经有了（CLAUDE.md 文件规则），缺的是执行**：比赛交件后做一次大扫除，`C:\dev` 只留 `minit-v2`

### [C-15] 小事集（有据可查，一并列出）
- 🟢 PROMPT 自身笔误：7.4 写 `register-store.tsx` 869 行，实际 **669**（`wc -l` 亲测；档案地图里的 669 才对）
- 🟢 deck p6 写 291 个测试，现在是 648——slide 后置没关系，别忘了一起改
- 🟢 eslint 21 problems 基准同意维持，但 `react-hooks/set-state-in-effect` 那类值得在交件后清零
- 🟢 `/money` 空状态其实做了（`register-receipts.tsx:288`）——7.7 截图里「请先选择机构＋5 笔捐款」并存的场景，我在现行代码里没找到还能复现的路径，标为「可能已修，需真机确认」
