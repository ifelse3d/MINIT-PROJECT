# 01 · 评分 — Claude（2026-08-24）

> 评审方式：读了 PROMPT 里点名的档案＋自己抽查的程式码；亲自跑了
> `tsc`（0 错）、`eslint`（21 problems）、`vitest`（648/648 过）、`npm run build`（成功）、
> `npm run check:migrations`（16、17 未套用；20 张表 0 笔）、`npm run check:ai`；
> 并在 J 当面授权下真的跑了 `npm run eval`（**92.9%，1 个 invented**）和
> `npm run eval:tools`（**18/18 全对，OpenAI 线上 API 首次实测通过**）。
> 登入后的画面我和其他评审一样看不到（帐号是 J 的），UI 结论来自读 `.tsx`。

---

## 表 A · 比赛评审角度（MAIC 官方评分表，满分 100）

**前提：按「今天这个状态直接交出去」打分。** 交出去的东西＝deck PDF＋摘要＋AI 声明；
无 Live URL、无影片、无截图、repo 私有（＝artifact 视同没交）。

| 项目 | 权重 | 实得分 | 理由 | 加 5 分最快的做法 |
|---|---|---|---|---|
| Technical Feasibility | 25 | **14** | 底子是真的：648 测试全绿、build 过、我今天实测 eval 92.9%、function calling 对真 API 18/18。但评审**一样都看不到**：无 URL、无影片、repo 私有；而核心卖点「读手写」的证据是零（`eval/real/` 不存在，10 张标准案例全是印刷体，我亲眼开过 `case-01` 的 PNG） | 部署 Vercel＋把带 model id 的 eval 报告和 3 张真手写 before/after 放进公开 artifact |
| Commercial Viability | 25 | **13** | 好的一半：90,224 这个市场数字有官方出处有日期；RM39/99/188＋RM150 分层在 `summary-onepager.md` 是自洽的。坏的一半：`business-model.md:72` 那行 RM200 僵尸价格还活着；三个互相矛盾的成本数字；**我今天跑 `check:ai` 实测：现在这套模型分流「不属于任何已定价情境」，等于已公布的 73.4% 毛利不适用于现况**；J 被问三次价格未拍板；0 个付费或试用客户 | J 拍板一套价格 → 删 RM200 行 → `unit-economics.ts` 加当前 routing 的情境，全 repo 只留一个数字 |
| Industry Relevance | 20 | **15** | eROSES 数位化＋e-Invois 扩围是真实的国家级推力，T5 的 multilingual LLM／civic tech 完全对口；这是全案最稳的一格 | deck/摘要里把「90,224 个社团＝公民自治的公共服务末梢」这句 T5 翻译讲死，附 JPPM 截图 |
| Scalability | 15 | **9** | HQ＋分会树状＋RLS 多租户从第一支 migration 就在（架构是真的）；但 0 个社团在用，20 分堂试点还没开始，第 8 个证据点空白 | 试点庙宇网络先建 3 个真实分会帐号，展示总会视图的真截图 |
| ESG / National Impact | 15 | **9** | 「让不会英文的义务财政也能合规」是好故事，PDPA-by-design 在程式里查得到（遮名、不落日志）；但 `legal/` 45 处占位符、无同意书、零实际影响数据 | 填掉 legal 占位符＋一份真实社团的第一张合规文件当案例 |
| **总分** | **100** | **60** | | |

### 加答两题

**1. 以现在这个状态交出去，会不会进初赛（2026 年 9 月）？**

会「被评」，大概率不会「出线」。三样必交（deck、摘要、AI 声明）都有草稿，交得出去就进入初选评审；
但初选是线上评、top 10 进十月 Demo Day —— 对手里只要有几队带 Live URL＋demo 影片，
一个「什么都点不了」的队很难挤进前 10。
**另外这份 PROMPT 没写、但比一切都重要的一件事：`competition-facts.md:24` 写着「300 队上限、先到先得」，报名费 8/9 起是 RM500。团队到底报名了没有？如果还没有，其他所有事都排它后面。**

**2. 一个评审能问出来、最伤的一个问题是什么？**

> **"Your whole pitch is that it reads messy handwriting. Show me one real handwritten page, before and after."**

现在的答案是拿不出来——`eval/real/` 不存在，evidence-tracker 第 1 格空白，
连内部文件（`docs/产品缺口盘点.md`）都自己写着别再犯「95.2% 量的是印刷体」这个错。
第二伤："How many societies use it today?" —— 答案是 0（20 张表 0 笔，我实测）。
第一个问题更伤，因为它杀的是**主张本身**，而且七天内本来是修得掉的。

---

## 表 B · 普通民众角度（35 岁上班族秘书，1–10）

| 项目 | 分数 | 理由（画面／档案） |
|---|---|---|
| 第一次打开，我知道要做什么吗 | **7** | `page.tsx:155` OrgPrompt 是全页最大的一块字、说人话（「告诉 Minit 您机构的名字…大约只要一分钟」）；「一个门」的 AskBox＋HowItWorks 引导都在。扣分：没有「新社团还是旧社团」的分流（盘点 19），三语并排让首屏字量翻三倍 |
| 我看得懂画面上的字吗 | **5** | `<Tri>` 预设三语全开串成 `Wang · 财务 · Money`（`language-provider.tsx:16,83`），侧栏再被 `truncate` 切成 `拍照与核对 · Phot...`（`sidebar.tsx:92,138`）。右上角其实能关到只剩中文——但没有任何提示告诉他那三颗 BM/中/EN 是干嘛的。eROSES、e-Invois、paste-pack 这些词在画面上裸奔 |
| 我找得到我要的功能吗 | **6** | 侧栏分组＋每步有自己网址是对的；但 `/minutes` 分页写 **2**、进去区块标题写 **3**（`attendance-review.tsx:145` vs `section-tabs.tsx:83` 的 `{i+1}`），一个照编号找路的人会怀疑自己走错 |
| 出错的时候我知道怎么救吗 | **6** | `user-errors.ts` 是真下过功夫的：三语、说具体动作（「裁掉空白只留有字的地方」）、不甩锅使用者。8/20 走查那个「画面收下、伺服器拒绝、还说是 Minit 的错」的根已经修了（`field-row.tsx:119` 现在是 `<select>`、`:141` 是 date picker）。扣分：`serverError` 这句对少数残余场景仍是误导 |
| 我敢不敢把真的捐款资料放进去 | **4** | 收据开出后不能删（对），但**打错字没有作废重开的路**（§9.6 属实）；PDPA 同意书没有、`legal/` 45 处占位符；行事历／交接在 migration 16 跑之前只活在一台浏览器里（我实测 NOT YET） |
| 我会不会推荐给我的社团 | **5** | 核心流程若真能走完，这是真痛点真解药；但今天我自己会先被「中途不能存草稿」（盘点 9）劝退一次 |
| 手机上能用吗 | **5** | Capacitor 壳、mobile-nav、字级设定都在；「手机选不到 PDF」那类 bug 前天才修（`143fb42`），E2E 是零，**没有任何一次真机走完全程的纪录** |

**小计：38/70**

---

## 表 C · 68 岁、小学程度（1–10）

| 项目 | 分数 | 理由 |
|---|---|---|
| 字看得清楚吗 | **6** | 四段字级、rem 化（`appearance-provider.tsx`）是真无障碍设计；但深色模式边框对比 2.03:1（`docs/无障碍对比度审查.md` 自己记录的），画面会「糊成一块」 |
| 按钮按得到吗 | **6** | 主线按钮大；捐款卡片里「修改资料／删掉这一笔」一排小按钮对抖的手不友好（`register-receipts.tsx:351` 起） |
| 看得懂中文的部分吗 | **6** | **能关到只剩中文是这个人群的救命功能**——但他自己发现不了右上角那三颗小字母按钮；预设三语全开对他是最差的预设 |
| 「拍照 → 核对 → 存起来」我自己走得完吗 | **4** | 步骤锁住会解释原因（好）；但核对步骤是一整张栏位表，而且**中途走开就重来**（盘点 9）——对一个每步都要戴老花镜确认的人，这几乎保证走不完 |
| 哪一步会让我不敢按，怕按坏 | — | 「开收据」：画面明说开了就改不了、又没有「开错了怎么办」的出路——他会停在这里去等孙子。其次是任何写着会用掉 AI 额度的按钮 |
| 哪一步会让我放弃，去叫孙子来 | — | 第 0 步：登入本身（email＋密码，而他可能没有 email；「邀请第二个人」流程也还没有，盘点 11）。进得去的话，则是第 2 步那张核对表 |

**小计（可打分四项）：22/40**

---

## 三个角度里哪一个最低？应该先救哪一个？

**最低的是 C**（68 岁财政），而且他还有两项根本走不到能打分的地方（登入、作废）。

**但应该先救的是 A——用的却是 B/C 的药。** 理由：距截止 7 天，A 的致命伤
（手写零证据、无 Live URL、无真实使用）恰好只能靠「把 B 走的那条主线用真资料真设备走通一次」来救。
拍真手写→跑 eval→部署→建真社团→截图，这一串每一步同时在替 B 和 C 排雷。
单独去救 C（字级、文案、作废流程）在 7 天内救不动 A，反过来救 A 的过程会自动暴露 B/C 的前三个雷。
