# 04 · 我没能验证的 — Claude（2026-08-24）

> 按 PROMPT 第 12 节要求分四类。
> 特别声明：我就是「写这套程式的同一个 AI」的另一个 session。J 明示要我不受此前工作影响；
> 我的做法是**对 PROMPT 里每一条载重事实重新亲测**，下面第一节就是清单。
> 即便如此，「同一个模型的盲点可能相同」这个风险我消不掉，只能标出来——
> 这也是三家评审应该互相比对的理由。

---

## A. 真的跑过／打开看过的（可复现）

| 事 | 我看到的 | 凭据 |
|---|---|---|
| `npx tsc --noEmit` | 0 errors | 今日实跑 |
| `npx eslint .` | 21 problems（20E＋1W） | 今日实跑，与基准一致 |
| `npx vitest run` | 46 档案、648 测试全过，912ms | 今日实跑 |
| `npm run build` | 成功（exit 0） | 今日实跑（背景） |
| `npm run check:migrations` | 1–14 APPLIED；**16、17 NOT YET**；20 张表全部 0 笔 | 今日实跑 |
| `npm run check:ai` | 四任务分流如 01 所述；**「not one of the priced scenarios」**；GEMINI/OPENAI/XAI key present、ANTHROPIC absent | 今日实跑 |
| `npm run eval`（J 授权，花真钱） | **92.9%（117/126）、invented=1**、model=gemini-3.5-flash-lite（报告印的是解析结果不是环境变数） | `eval/reports/eval-2026-08-24T10-21-05.md` |
| `npm run eval:tools`（J 授权） | **18/18 全对**，openai:gpt-5.6-luna，真 API 首次实测 | 今日实跑输出 |
| `eval/cases/case-01-minutes-agm/input.png` | 干净印刷体，白底黑字 | 亲眼打开 |
| `eval/real/` | 不存在 | `ls` 报错 |
| git | 43 commits，起点 `ba51714` 2026-08-21「Minit — clean tree」，HEAD `df7cc87` | `git log` |
| `competition/screenshots/` | 只有 README | `ls` |
| `competition/evidence-tracker.md` | 8 格全 ☐ | 读档 |
| `business-model.md:72` | RM200/月那行还在表格里 | 读档 |
| 步骤编号 | `notes-review.tsx:67,255`＝1,2；`attendance-review.tsx:145`＝3；`minutes-document.tsx:94,325`＝4,5；tabs 用 `{i+1}`（`section-tabs.tsx:83`）；`/money` 侧 1–4 对齐 | grep＋读档 |
| 侧栏截断 | `sidebar.tsx:92,138` truncate | 读档 |
| 语言预设 | `language-provider.tsx:16` 三语全开、`:83` join(" · ") | 读档 |
| API 边界 | `proxy.ts:87` 未登入 `/api/*` 回 401；`intake/route.ts:123` 先扣费后呼叫；`usage.ts:178` fail-closed | 读档 |
| `register-store.tsx` 行数 | **669**（PROMPT 7.4 写 869 是笔误） | `wc -l` |
| `/money` 空状态 | 存在（`register-receipts.tsx:288`） | 读档 |
| `field-row.tsx` | `:119` `<select>`、`:141` `type="date"`——8/20 走查的 enum 病根已修 | 读档 |
| deck 现行版 | 10 页、讲的是本产品（Ifelse 队名、90,224、问题→架构→money trail），p5 对准确率的措辞是诚实的（「being measured…figure ships with our artifact」） | 浏览器逐页看了 p1–p6 |
| `r1-draft/05_DECK_10_SLIDE_CONTENT.md` | 内容是另一个产品（报修工作流），每页 FICTIONAL | 读档 |

## B. 推的（有推理链，未直接看见）

- **表 B/C 的每一格分数**：来自读 `.tsx` ＋ J 的走查纪录，不是我看着画面打的。
  这正是 PROMPT 6.3 承认的「程式码稽核看不到画面」限制——分数应被当成假设
- **「示范资料与真实资料界线」（7.7）大概率已修**：从 `register-receipts.tsx:288` 的空状态＋
  `minutes-store.tsx:307` 的注解推的；没有真机复现就不能算数
- **电话截断→WhatsApp 发错**：链路（eval 失败样本 → `buildWaMeLink`）都看到了，
  但没有真的按下那颗按钮验证末端行为
- **评分表 A 的分数**：以「今天直接交」为前提对着评分权重推的；MAIC 评审的实际口味没人知道

## C. 猜的

- 初选出线线在哪、对手强度如何（300 队上限、T5 赛道有多挤）——纯猜
- 三家评审比对后会不会高度重合（我猜会：这份 PROMPT 事实密度太高，容易把所有评审引到同一批结论上——所以我特意把力气花在它没写的地方）

## D. 根本查不到的

- **登入后的真实画面**（帐号是 J 的；创建帐号属于我不该做的操作）——所以 5.6 那条对我也成立
- **真实手写的表现**——`eval/real/` 不存在，无从测
- **Supabase 端 RLS policy 与函数的实际形态**（`prosecdef`、trigger 等）——PROMPT 边界禁止碰线上库；`check-migrations` 的「人眼」清单我只能原样转告
- **J 有没有报名比赛**——全部文件里没有一个字提到报名动作本身；这是我要问 J 的第一个问题
- **`.env.local` 内容**——CLAUDE.md 明令不读，我没读（key 的存在性来自 `check:ai` 的 presence 输出）
- **另外两家评审写了什么**——`reviews/CODEX/` 已存在，我刻意没打开，维持独立性
