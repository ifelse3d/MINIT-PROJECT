# 04 · 我没能验证的 · GROK · 2026-08-24

这一份把「看见」「推出」「猜的」「根本查不到」分开。评分和建议里用到的事实，只应来自前两类；后两类如果进了 01/02/03，这里会点名。

---

## 1. 真的打开档案／跑指令看到的

### 指令（本机，`C:\dev\minit-v2`）

| 指令 | 我看到的 |
|---|---|
| `git log` / `git remote` / `git status` | `HEAD` = `df7cc877`，`main` 跟踪 `origin/main`。远端 `https://github.com/ifelse3d/MINIT-PROJECT.git`。工作区干净，未跟踪的只有 `reviews/CLAUDE/`、`reviews/CODEX/`。最近提交从 2026-08-23 到 08-24。 |
| `node scripts/check-migrations.mjs` | 打的是 `https://fxhmbpxctwjqyvtdhmtp.supabase.co`。1–14 相关探针 APPLIED；**16 NOT YET**（`events_meetings.client_id`、`remittance_batches.collector_name`、`deadlines.client_id` 皆 `42703`）；**17 NOT YET**（`member_groups` `PGRST205`）。`cari_minit` RPC APPLIED。 |
| 同一支脚本印的 row counts | **20 张表全部 0 笔**，包括 `orgs`、`donations`、`receipts`、`minutes_docs`、`ai_usage`。 |

我 **没有** 跑 `tsc` / `eslint` / `vitest` / `build`。这四项沿用 prompt 5.1 的 2026-08-24 结果当背景，不当成我自己量的。

我 **没有** 跑 `npm run eval`、`eval:tools`、`bench`。

### 打开看过的档案（部分，评分用到的都在这里）

- `src/proxy.ts` 全文。公开路径四条。注释写明 8/21 删掉 competition demo。
- `src/app/page.tsx`、`ask-box.tsx`、`how-it-works.tsx`、`login/page.tsx`、`login/glass.tsx`
- `src/components/language-provider.tsx`、`appearance-provider.tsx`、`nav-items.ts`、`section-tabs.tsx`、`page-section.tsx`
- `src/components/v2/sidebar.tsx`、`mobile-nav.tsx`、`app-shell.tsx`
- `src/app/minutes/minutes-chrome.tsx`、`minutes-store.tsx` 头部、`minutes-storage.ts`、`notes-review.tsx`（含 step 1/2、FieldRow choice）、`before-reading.tsx`、`attendance-review.tsx:145`、`minutes-document.tsx`（step 4/5、allReviewed）、`actions.ts` 头部
- `src/app/money/register-store.tsx`（localStorage 键、deleteDonation、issueReceipts 的 no_org 本地发号）、`register-receipts.tsx:280-390`、`money-chrome.tsx`、`actions.ts:79` `PREFIX = "MIN"`
- `src/app/filings/filings-view.tsx:46-68`
- `src/app/agm-pack/agm-pack-review.tsx`、`src/app/api/agm-pdf/route.ts:73`
- `src/app/api/intake/route.ts:243-248`（无 glossaryBlock）
- `src/lib/paste-pack.ts`、`receipts.ts:197-299`、`user-errors.ts`、`sample-data.ts`、`unit-economics.ts` 头部、`meeting-types.ts`、`standard-deadlines.ts:54-67`、`minutes-compose.ts` 头部、`minutes-draft.ts` 头部、`einvois-xlsx.ts:1-24`、`org-tools.ts:1-80`、`use-persistent-state.ts` 头部、`db/first-run.ts`
- `src/prompts/extract-meeting-notes.ts` 全文
- `src/app/layout.tsx:14-73`（Inter latin、`lang="ms"`）
- `capacitor.config.ts`
- `scripts/seed-demo.ts` 头部
- `eval/cases/case-01-minutes-agm/input.png`（印刷体，我看见图）
- `eval/cases/case-04-minutes-mixed/case.json`
- `eval/reports/SUMMARY.md`、`eval/reports/eval-2026-08-24T10-21-05.md`
- `competition/competition-facts.md`、`evidence-tracker.md`、`business-model.md`、`r1-draft/03_CLAIM_LEDGER.md`
- `docs/HOW_MINIT_WORKS.md`、`docs/产品缺口盘点.md`（含 08-23 更新区块）、`docs/无障碍对比度审查.md` 头部、`DEPLOY.md` 头部、`CLAUDE.md`、`STATE.md` 头部、`legal/*.md` 的 `[[ ]]`
- 官网 `https://maicnexus.com/en`（2026-08-24）：初赛问题原文、T5 描述、100→30、截止 8/31

### 我看见、因此写进 01/02 的关键事实

- 登入墙、demo 已删
- 空库、migration 16/17 未跑
- 三语默认 + truncate
- `/minutes` 分页号码 vs 区块号码
- 登记簿 localStorage、键无 org_id
- filings 读 localStorage
- wa.me 文案声称 PDF 已附上
- 发号写死 MIN
- intake 不带词库
- 08-24 eval 92.9% / invented 1
- case-01 是印刷体
- AGM 仍是 sample flag
- Inter 无中文子集
- BARE_ROUTES 只有 `/login`
- 无 android/ios 资料夹
- Confirm email ON 写在 login 注释里（见第 3 节：这是注释，不是我登录 Supabase 控制台看到的）

---

## 2. 推的（有推理链，没直接看见运行时）

这些进了 02/03，标成推断。

| 结论 | 推理链 | 可能错在哪 |
|---|---|---|
| 评审打开成品 = DQ | 规则（competition-facts + 官网）× `proxy.ts` 只有四条公开路径 × 没有 Vercel 纪录 × GitHub 公开抓 404 | 他们可能另有一个我没看到的公开 URL（不在这棵树里） |
| GitHub 是 Private | `https://github.com/ifelse3d/MINIT-PROJECT` 公开抓 404。未登录状态下 Private 和「不存在」长一样 | 也可能是换了名字／删了。本地 remote 仍指向它 |
| 68 岁走不完拍照→保存 | 把注册规则、建机构跳章程、AskBox 禁用、四页核对、allReviewed、马来文产出串成一条路径 | 如果旁边有孙子、语言已关成中文、机构已建好，路径短一截。我评的是「她自己、第一次」 |
| 首页门比 /minutes 容易认错字 | intake 不传 glossaryBlock，extract-minutes 传 | 词库是空的时候两条路等价。空库上现在词库一定是空的 |
| 切机构会混捐款 | 键名是全局的 `minit:money:donations:v1`，没有 org_id | 如果产品强制一人一机构，混不到。代码允许 HQ 树，所以会混 |
| Confirm email 现在是 ON | `login/page.tsx:200-207` 的注释当现场纪录 | 注释可能过期。要以 Supabase 控制台为准 |
| 08-24 eval 真的是 flash-lite 跑的 | 报告自己写 `Model: gemini:gemini-3.5-flash-lite ← what extraction ACTUALLY ran on`。我没重跑 | 报告可能被人改过。档案时间 2026-08-24T10:21:05 |
| 现场 Demo Day 会当场试手写 | T5 + 产品自己把「手写混语」当卖点 + 10 月是 live demo | 评审也可能只看影片 |
| RM 99 是对的价格 | 庙委会能不经 AGM 批；RM 39 偏弱；RM 200 文件已宣布死亡 | **没做过支付意愿。这是建议不是测量。** |

---

## 3. 猜的

这些 **没有** 当成 01 的分数依据。如果 03 提到了，这里收回成「猜测」。

- 20 个分堂的庙「其实已经口头答应了」。代码和文件都说 7/29 是 0 个社团。我当 0。
- J 会不会在 8/31 前自己部署。不知道。
- Gemini 是否仍是付费 Tier 1。文件写 8/20 眼睛确认过。我没打开 Google AI Studio。
- `20260728000000` / `20260729000000` / `20260824000000` 三支「探针看不见」的 migration 到底跑了没。脚本自己说要眼睛看。我没进 SQL Editor。
- 深色模式下 `--v2-border` 现在是不是还是 2.03:1。审查文件是 8/3–8/7，之后玻璃 token 改过。我没做测色。
- HEIC 在目标机型上是不是还是问题。代码有用户文案，我没拿 iPhone 试。
- function calling 第一次打 Gemini 会不会 400。形状测试绿了。真 400 是猜的风险，不是已观测到的失败。
- 6 吋 Android 上 `truncate` 会切成 prompt 里写的 `Phot...`。逻辑上会；我没截到那一屏。
- Inter 在马来文 Android 上中文会豆腐。取决于系统后备字体。我没真机。

---

## 4. 根本查不到的

### 登入之后的真实画面

prompt 11 节说了：没有帐号。我没要、也没用 J 的凭证。

所以我 **没看见**：

- 登录后首页在空库上的实像素（OrgPrompt 的 JSX 我读了，玻璃和截断没看见）
- `/minutes` 四页、`/money` 五页的实机
- 语言切到「只留中文」之后侧栏还宽不宽
- 字级 xlarge 在 6 吋上是否仍然太小
- AI dock 打开后是否挡住「拍照」
- 示范资料按钮按下去长什么样
- 真的拍一张纸，Gemini 在这个专案的金钥下会回什么

01 表 B / 表 C 是 **从 JSX 推画面**，不是眼睛。这是这份评审最大的洞。J 如果把「只开中文、6 吋、空库、登入后」的 8 张截图丢过来，表 C 的分数可能微调，**总症不会变**——路径长度和确认表结构在代码里。

### 线上 Auth 设定

Confirm email、密码规则、SMTP、redirect URL。代码和注释说了一套。控制台可能是另一套。`login/glass.tsx` 自己警告这两边会漂。

### 真实手写准确率

`eval/real/` 不存在。我看见的唯一一张 input 是印刷体。08-24 报告也是那 10 个 case。**产品核心能力 = 未知。**

### 真 eROSES / 真 MyInvois 门户

`eroses-map.ts`、`einvois-xlsx.ts` 是照文件写的。我没登录 ROS 或 LHDN。8#15 仍然是人的工作。

### 厂商账单、真实 token、平均工具回合

`ai_usage` 0 笔。4 次呼叫扣 1 次额度，对毛利的伤害是模型不是测量。

### Playwright / 视觉回归 / 真机

没有。46 个测试档我没打开去数，prompt 说 46 全是单元测试——我抽看了 `receipts.test.ts`、`extraction.test.ts`、`nav-items.test.ts` 的存在，没把 46 个档案逐一分类。

### 旧 Supabase 树 `ivsqiycvahcevzgokgvc`

prompt 说 8/20 还有 4 个 org。我没连那一棵。不知道资料还在不在、为什么没迁。

### 另外五棵 `C:\dev\` 下的副本

我没进去比对 `minit` / `minit-champion-overnight-*` 等是否比 v2 多出能用的 demo。评分只针对 `minit-v2`。

### 比赛门户「此刻」的页数／字数限制

competition-facts.md 是 7/9。R1 ledger CLM-030 说 8/9 审到过 ≤12 页、摘要 ≤500 词。官网今天我看到了赛程和 T5，**没打开申请表逐步确认字数**。交件前 J 必须自己再看一次 portal。

### 我刻意没读的

- `reviews/CLAUDE/`、`reviews/CODEX/` 的正文。避免被带节奏。知道它们存在（git status 未跟踪）。
- `.env.local` 的值。规则禁止。
- PPT / PDF deck 的每一页。你说系统优先。我只确认了 competition 目录里有 8/21 的 PDF/PPTX，以及 r1-draft 的 claim ledger 在讲另一套产品。

---

## 5. 哪些 01/02/03 的句子必须降权

| 句子 | 级别 |
|---|---|
| 总分 41、Technical 8 | 比赛评审口径（15 分钟、看门）。若评审能读 repo，Technical 可到 15，总分约 48。两种都写在 01 了。 |
| 「不会进初赛」 | 取决于 8/31 前有没有公开 URL。以 **今天** 为真。 |
| Confirm email ON | 注释级。要控制台确认。 |
| 68 岁 2/10 走不完 | JSX 推理。要真机才会变成测量。 |
| RM 99 | 建议，不是市场研究。 |
| 「路灯叙事会让评审判不专业」 | 推断。如果交件包完全不提 r1-draft，评审根本看不到。风险是有人把旧 PDF 传上去。 |

---

## 6. 如果要填这个洞，J 只需做的最小集合

1. 6 吋 Android、只留中文、登入后空库：首页、/minutes、/money/receipts、汉堡选单，各一张截图。
2. Supabase Authentication 截图：Confirm email、密码规则。
3. 4 张真手写。
4. 一句：8/31 的成品连结会不会无登入。这决定 01 的 DQ 判断还能否翻盘。
