# 02 · 问题清单 · GROK · 2026-08-24

按严重度。第 5–9 节已经写过的，同意就一句，不重述。**没写过的排前面，那是这份评审的价值。**

严重度：🔴 挡住上线或比赛 / 🟠 会流失真实使用者 / 🟡 不好但能忍 / 🟢 小事

---

## 第 7 节那 7 条（先判，再列新的）

### [7.1] migration 16、17 没跑，日历／交接／分组只活在这台电脑
- **严重度**：🔴
- **成不成立**：**成立。** 不是转述。我今天跑了 `node scripts/check-migrations.mjs`，针对 `fxhmbpxctwjqyvtdhmtp.supabase.co`：第 16 支 `events_meetings.client_id` / `remittance_batches.collector_name` / `deadlines.client_id` 三个探针都是 `42703`；第 17 支 `member_groups` 是 `PGRST205`（表不存在）。20 张表全部 0 笔。
- **在哪**：线上库；代码写端在 `src/app/calendar/actions.ts`、`src/app/money/custody-actions.ts`、`src/app/members/group-actions.ts`
- **谁会踩到**：都会。换手机的财政会以为系统把交接吃了。
- **为什么会变成这样**：给 J 的工具要人手同步，然后两边一起忘。8/24 修了工具，没修库。**推测**：J 还没被明确用一页纸叫去 SQL Editor 贴这两支。
- **我会怎么做**：不要再写工具。写一张「只贴这两段 SQL、跑完把截图发回来」的纸，J 今天做。做完之前，画面上凡是写进这两张表的按钮，要明说「现在只存在这台手机」。

### [7.2] 三语预设全开，侧栏切成 `...`
- **严重度**：🔴（对 68 岁是门；对评审是「他们没想过谁在用」）
- **成不成立**：**成立。** `language-provider.tsx:16` `DEFAULT_PREFS = { bm: true, zh: true, en: true }`；`Tri` 用 `" · "` 串（`:80-83`）；`sidebar.tsx:92, 138` `truncate`。
- **谁会踩到**：68 岁财政 / 手机评审
- **为什么会变成这样**：CLAUDE.md 第 9 条把「三语 UI」写成同时显示。那是给工程师看「我们支援三种语言」的证明，不是给使用者看的界面。**推测**：从来没有人拿 6 吋真机、只开中文、看过第一屏以外的东西。
- **我会怎么做**：第一次打开只问一次「BM / 中 / EN」，只留一种。`Tri` 只渲染当前这一种。侧栏不再需要 truncate。三种同时显示变成设置里的「进阶」。

### [7.3] 为什么叫「拍照与核对」
- **严重度**：🟠
- **成不成立**：**成立。** `minutes-chrome.tsx:20`、`nav-items.ts:86`。
- **为什么会变成这样**：信息架构按 **系统工序** 命名（读、核对、产出），不按 **人要完成的事** 命名。Money 同一病：首页「捐款与收据」≠ 侧栏「登记与收据」≠ 分页「开收据」（`money-chrome.tsx:25` vs `nav-items.ts:93`）。
- **我会怎么做**：组名「会议记录」，底下两步：「拍笔记」「做好的文件」。Money：「收钱」「收据」。不要让她学你们内部的四步。

### [7.4] 排版很奇怪
- **严重度**：🟠
- **成不成立**：**成立。拆页的动机对，拆法错。**
- J 说「不要所有功能都在一页」。正确反应是 **一页只做一件人的事**。实际做的是 **一个工程模块变成一个网址**：读账、开收据、交接、税务、历史、拍照、出席、文件、历史。她要开一张收据，要先理解自己在第几步。
- 每一页同一件外壳（`page-section.tsx`：玻璃卡、号码、标题、一句说明、下一步）让 9 个画面长得一样。老人靠位置记忆，不是靠读标题。9 张一样的卡 = 迷路。
- **我会怎么做**：英雄流程只要两屏——「照片／账页」和「结果」。核对发生在结果上（标黄的才要碰）。其余（交接、税务、历史、章程）进「更多」。

### [7.5] 一笔捐款一张卡片，多人就太多
- **严重度**：🟠
- **成不成立**：**成立。** `register-receipts.tsx:308-309` 无搜索、无排序、无翻页、无批次。一场庙会 200 笔 = 100 列。默认遮名之后更没法扫（`T*** K**`）。
- **不是完全没路**：`type-donations.tsx` 可以整批打。但 (a) 她要从「开收据」页找进去；(b) 那张表 `min-w-[40rem]`，6 吋手机横滑。
- **我会怎么做**：≥8 笔改成列表（姓名一行、金额一行、状态点）。搜索放顶上。打字整批保留，做成手机上「一列一列往下」不是 Excel。

### [7.6] 号码对不上；被流程绑住
- **严重度**：🟠
- **成不成立**：**成立。** 分页号码 `section-tabs.tsx:83` `{i+1}`；区块号码手写。`/minutes`：分页 1 里面是区块 1+2，分页 2 是区块 3，分页 3 是区块 4+5。`/money` 碰巧对齐，所以这不是设计，是没对过。
- 另外一条 prompt 没写到：`NextStepLink` 遇到 `blockedReason` **把连结拿掉**（`page-section.tsx:78-83`）。CLAUDE.md 第 13 条写「被锁的步骤仍是真页面、真网址」。代码只对 **分页轨** 做到，对页脚下一步没做到。
- **我会怎么做**：删掉 `page-section` 的 `step` 数字。只留分页轨。能做的下一步是按钮，不能做的是同一颗按钮加一句「要先……」。

### [7.7] 整个不人性化
- **严重度**：🔴（产品总症，不是某一页）
- **成不成立**：**成立。J 讲不清楚是因为这不是一个 bug，是口气和结构。**
- 翻译成可以修的东西，见下面 **[N1]**。

---

## 这份 prompt 没写到的问题（按严重度）

### [N1] 系统在用审计员的口气跟义工说话 —— 7.7 的具体化
- **严重度**：🔴
- **在哪**：几乎每一页。样本：
  - `minutes-document.tsx:83-88`「还有 N 项要核对。您还没确认的内容，Minit 不会拿去写正式文件。」
  - 每格三个按钮：没错 / 修改 / 笔记里没写（`notes-review.tsx:270-273`）
  - 保存的条件是 `allReviewed && isReal`（`minutes-store.tsx:610, 674`）—— **全部** 栏位都点过才能看到正式文件
  - 示范有水印、草稿有水印、AGM 有「CONTOH — JANGAN GUNA」、没有机构就「请先选择」、没有额度就「本月用完」
- **谁会踩到**：68 岁 / 35 岁 / 现场评审
- **会发生什么**：eROSES 测试写着「力气从 AI 流向人」。执行出来是 **空表换成了确认表**。一场 20 项的会议 = 60 次点击。她觉得自己在填政府表，只是表是 AI 先填好的。
- **为什么会变成这样**：Hard Rule 1「AI 不准编」被执行成「人必须对每一个原子栏位表态」。正确的执行是：AI 交出一份文件，**不确定的标黄**，人只碰黄的，其余默认接受。
- **我会怎么做**：核对页默认「看起来对的已经算接受」。只有 `check` / `missing` 要人动手。顶部一颗「全部没问题」。正式文件 **随时看得到**，上面盖「未确认」水印，而不是不给看。

### [N2] 比赛成品是登入墙；以前的公开 demo 被删掉了
- **严重度**：🔴 直接 DQ
- **在哪**：`src/proxy.ts:10-15, 38`；`src/components/v2/app-shell.tsx:25` `BARE_ROUTES = ["/login"]`
- **会发生什么**：评审打开连结 → 登入页。规则视为没交。`/terms` `/privacy` `/reset-password` 不在 BARE_ROUTES，评审点同意条款会掉进全套侧栏，点任何一项再被弹回登入。
- **为什么会变成这样**：8/21 以「生产环境不该有只在本机打得开的分支」删掉 `/competition-demo`。这是工程师的干净，换比赛的命。
- **相关**：`scripts/seed-demo.ts` 写好了（虚构总会 + 两个分会，密码 `MinitDemo#2026`），**线上 0 个 org，没跑过**。就算跑了，要登入，仍然 DQ。
- **我会怎么做**：见 03。公开 `/demo`，cookie 标 `is_demo`，资料全是虚构，大字写 DEMO。不要叫评审注册。

### [N3] 注册要电邮确认 + 复杂密码；使用者没有电邮习惯
- **严重度**：🔴 对真实财政；也杀 demo
- **在哪**：`login/page.tsx:200-207`（注释写明这个 Supabase **Confirm email = ON**）；`login/glass.tsx:36-46` 大写+小写+数字+符号
- **谁会踩到**：68 岁 / 评审被要求「自己试试」
- **DEPLOY.md:69** 自己写过：比赛成品 Confirm email 应该 OFF。现在是 ON。
- **我会怎么做**：比赛专案：确认电邮关、密码只要 8 位。真客户专案再打开。不要用同一个 Supabase 项目既给评审又给真庙。

### [N4] 08-24 eval：印刷体已经 invented = 1，对外数字没改
- **严重度**：🔴 比赛 §12 不实陈述风险
- **在哪**：`eval/reports/eval-2026-08-24T10-21-05.md`（92.9%，117/126，invented 1）；`eval/reports/SUMMARY.md:43-46` 仍写 95.2%、invented 0
- **发明的是什么**：case-04，把数字栏 `Kutipan derma bulan Jun: RM830.00` 再写进决议。黄金答案里它是 figure，不是 resolution。
- **谁会踩到**：评审问「你们说 AI 从不发明？」
- **为什么会变成这样**：军规写在 prompt 里（`extract-meeting-notes.ts:25`），没有写在抽样后的硬校验里。`checkCoverage` 防的是「写正式文件时漏条」，不防「抽取时多条」。
- **我会怎么做**：对外立刻停用 95.2%。可以说「印刷体样本 10 张，最近一次 92.9%，有 1 个发明栏，我们标出来了」。抽取后加一条：figure 的金额不得作为 resolution 的新条（确定性代码，不是再问模型）。

### [N5] `/filings` 读的是这台手机的未确认草稿，不是已保存的会议记录
- **严重度**：🔴 假申报
- **在哪**：`src/app/filings/filings-view.tsx:46, 62-68` `localStorage["minit.minutes.v1"]`
- **会发生什么**：秘书把还在改的出席人数贴进 eROSES。换一台电脑，呈报页是空的，历史里其实有已确认的会议。政府页和正式记录不是同一个来源。
- **为什么会变成这样**：当初 filings 是「薄聚合页」，直接读当时唯一的 store。会议记录后来能存进 `minutes_docs` 了，这页没改。
- **我会怎么做**：只读最新一份 **已确认** 的 AGM/EGM。没有就不给贴，不要给一个看起来像真的空表。

### [N6] 捐款登记簿不从资料库读回来；localStorage 也不按机构隔离
- **严重度**：🔴 钱
- **在哪**：`register-store.tsx:196-210` 键名 `minit:money:donations:v1`、`minit:money:batches:v1`。开收据会 `issueAndSaveReceipts` 写入 DB，**回来不 hydrate**。
- **同类键**：`minutes-storage.ts:20` `minit.minutes.v1`；活动、章程也是全库一把钥匙。
- **会发生什么**：
  1. 手机 A 开了收据，手机 B 登记簿是空的，历史里有收据。财政以为钱丢了。
  2. 同一部手机切总会 / 分会，捐款混在一起。下一张收据可能打在错误的机构名下。
- **为什么会变成这样**：HOW_MINIT_WORKS.md 第 3 节把 localStorage 写成「demo 权宜，接上 DB 就换」。收据那一半接上了，登记簿没换。键名没有 `org_id`，因为当初一人一机一个社团。
- **我会怎么做**：有 `org_id` 时登记簿从 `donations` 读。localStorage 只当离线草稿，键加上 `org_id`。没有机构禁止开收据（现在 `no_org` 会本地发号，`register-store.tsx:433-446`）。

### [N7] WhatsApp 对捐款人说谎：说 PDF 已附上
- **严重度**：🔴 对捐款人；也是 pitch 谎言
- **在哪**：`src/lib/receipts.ts:197-201` 只做 `wa.me?text=`；`:299` `(Resit PDF dilampirkan / PDF receipt attached)`。文案还是马来文（`:280` 函数名 `...Bm`）。
- **谁会踩到**：68 岁财政按「用 WhatsApp 发送」；捐款人收到一则没有附件的马来文。
- **为什么会变成这样**：v1 决定「不自动发」是对的（非官方库会封号）。有人在讯息模板里 **提前写了还没做的那一半**。
- **我会怎么做**：删掉「PDF 已附上」这行。改成「请把刚才下载的收据照片发到这个对话」。按钮顺序改成：先下载 PDF → 再打开 WhatsApp。不要在 7 天内接 WABA。

### [N8] 设定里的收据字号是摆设，真的开收据写死 `MIN`
- **严重度**：🟠（20 个分堂会撞号）
- **在哪**：设定写入 `orgs.receipt_prefix`（`orgs/actions.ts:180-204`）；开收据 `const PREFIX = "MIN"`（`money/actions.ts:79, 107`）
- **会发生什么**：两个分会都以为自己设定了 PSH / KLG，打出来都是 `MIN-2026-0001`。冻结触发器冻的是那个从没被用到的前缀。
- **为什么会变成这样**：UI 和发号不是同一天接上的。典型的「写完 ≠ 接上」，只是这次两边都有测试，测试测的是 `allocateReceiptNos` 的纯函数，不测 actions 有没有读 org。
- **我会怎么做**：`issueAndSaveReceipts` 读 `active.receipt_prefix`。没有设定就拒绝开，不要默默用 MIN。

### [N9] 首页「一扇门」比 /minutes 那扇门更贵、更笨、会漏词库
- **严重度**：🟠
- **在哪**：
  - 首页 `ask-box.tsx:188-215` → `/api/intake`，2 次额度（分类+抽取），**没有** `BeforeReading`
  - `/api/intake/route.ts:243-248` 呼叫 `extractMeetingNotesPrompt({ orgName, todayIso })`，**不传** `glossaryBlock`
  - `/api/extract-minutes/route.ts:103-106` 有传词库
- **会发生什么**：听产品的话、从首页拍照的人，会用掉两倍额度，而且会把「昶源」认成「湘源」——词库就是为这件事存在的。J 拍白板日期分不清的问题，首页那条路也没问。
- **为什么会变成这样**：一扇门是后来加的路由，抽取逻辑复制了一半。
- **我会怎么做**：首页拍照 = 跟 `/minutes` 同一条路（先问、1 次额度、带词库）。分类如果要做，用规则（档名／使用者选），不要多烧一次模型。

### [N10] 新用户建完机构被丢去章程；HowItWorks 对只做钱的人永远不消失
- **严重度**：🟠
- **在哪**：`create-org-form.tsx:19` `AFTER_CREATE_HREF = "/constitution?setup=1"`；`first-run.ts:17-31` 只数 `uploads` + `minutes_docs`，收据／捐款不算。
- **会发生什么**：财政建完「王爷庙」，第一眼是章程。她跳过，回家页，三步说明永远在，底下还有 e-Invois 死线。
- **我会怎么做**：建完问一句「您现在要做哪一件：会议记录 / 收据」。HowItWorks 在该机构有 **任何** 已确认资料（含收据）后关掉。没有机构不要显示税务死线。

### [N11] 中文不是一等公民：字体、html lang、章程搜索、姓名覆盖检查
- **严重度**：🟠
- **在哪**：
  - 字体：`layout.tsx:14-18` Inter latin；`globals.css` 字族无中文
  - `html lang="ms"` 写死
  - 章程搜索停用词只有 BM+EN（`constitution.ts:78-80`），中文「的／是／在」会稀释
  - `checkNames` 对中文稿跳过（`minutes-compose.ts:117-132`）——「小小小班」那种事故，中文会议记录没有网
- **谁会踩到**：目标使用者
- **我会怎么做**：Noto Sans SC 进 `next/font`；`lang` 跟语言偏好走；中文停用词；`checkNames` 对 CJK 跑字级 n-gram，不要直接跳过。

### [N12] e-Invois 画面说「上传这一个 Excel」，档案其实不是官方模板
- **严重度**：🟠
- **在哪**：`einvois-pack.tsx:100-112`「合并成一个 Excel……上传到税务局」；`einvois-xlsx.ts:11-17` 写明这是 **pre-fill pack**，官方是 11 个 sheet 的 `BatchSubmission-v1.xlsx`，故意不伪造。
- **会发生什么**：68 岁财政把 Minit 的 xlsx 丢进 MyInvois，门户报错，税务故事死亡。
- **我会怎么做**：画面改成「这是要抄进官方模板的数字，不是直接上传的档案」。给一张截图步骤。7 天内不要接 MyInvois API。

### [N13] AGM 整页是假的；名册写了没用
- **严重度**：🟠（评审随手点「年度大会」会判整站是假的）
- **在哪**：`agm-pack-review.tsx:31, 64` `sampleAgmPackParams`；`api/agm-pdf/route.ts:73` `SAMPLE_UNTIL_ROSTER_INGESTION = true`；`members/actions.ts:71` 其实已经能写 `committee_roster`
- **会发生什么**：呈报用的理事名单是 **这一次会议 AI 抠的** `office_bearers`，不是名册，也不是 IC 上的 `name_official`。假申报。
- **我会怎么做**：7 天内把 `/agm-pack` 从对外导航拿掉。呈报的理事名单改读 `committee_roster`。没有名册就缺漏，不要用会议上的活动分工顶上。

### [N14] 首页一扇门认得出、处理不了的纸
- **严重度**：🟠
- **在哪**：`classify.ts` 含 `attendance_sheet`、`expense`；`api/intake/route.ts:70-74, 163-176` 只接三种，其余 422。分类那一次额度已经扣了。
- **会发生什么**：她拍签到表，门说「读不了」，额度少了一次。
- **我会怎么做**：签到表进出席；支出先诚实说「现在还不会，不扣额度」。分类失败要退款（intake 现在 throw 不退，`intake/route.ts:265-269`；`/api/extract-minutes` 会退）。

### [N15] 没有邀请第二个人；也没有作废收据
- **严重度**：🟠 真实社团
- **在哪**：`members/page.tsx` 能列出 `members_roles`，没有邀请。`deleteDonation` 只删没有收据号的行（`register-store.tsx:379-384`）。全库搜不到 `void` / `reissue`。
- **会发生什么**：庙里财政 + 秘书是两个人。一个人注册，另一个只能看她的肩膀。打错 RM100 成 RM1000，没有合法出路，只能让号码断号或者继续错下去。
- **我会怎么做**：邀请可以很丑：设置页产生一个 24 小时码，对方注册时填。作废：`voided_at` + 新号，旧 PDF 盖作废。7 天只做作废文案和资料库，邀请可以交件后。

### [N16] 法律文件带着 `[[ ]]` 挂在公开路上，注册要勾同意
- **严重度**：🟠（PDPA 同意不是同意）
- **在哪**：`legal/privacy-notice-BM-EN.md`、`legal/terms-of-use-BM-EN.md` 约 40 处 `[[ ]]`；`proxy.ts:38` 这两页公开
- **我会怎么做**：比赛阶段：把注册同意改成「这是草稿，比赛演示用虚构资料」。真客户上线前填完 + 律师。不要假装已经同意。

### [N17] 抽取类 prompt 不在 injection-guard 里；词库是生拼进去的
- **严重度**：🟠
- **在哪**：`injection-guard.test.ts:34-79` 覆盖 ask/chat/classify/events/roster；**没有** `extract-meeting-notes.ts` / `extract-ledger.ts` / `extract-constitution.ts` / `draft-minutes.ts`。词库直接 `${glossaryBlock}`（`extract-meeting-notes.ts:69`）。
- **会发生什么**：章程或词库里出现「忽略以上指令」时，抽取模型比聊天模型更容易听话——它本来就被要求「照抄原文」。
- **我会怎么做**：词库走 `untrustedBlock`。护栏测试加上三份 extract prompt。

### [N18] 删机构清不掉这台手机
- **严重度**：🟠 PDPA
- **在哪**：`settings/actions.ts` 删 DB + storage；`use-persistent-state.ts:48-50` 坏掉的资料会 **复制** 一份 `key:unreadable-${Date.now()}`。捐款姓名、会议照片留在浏览器。
- **我会怎么做**：登出／删机构时清掉所有 `minit:` 键。不要为了「工程师能在 console 抢救」留下个资副本。

### [N19] Capacitor 不是 App；指向已删除的 `/v2`
- **严重度**：🟡 不要在 deck 上写「有手机 App」
- **在哪**：`capacitor.config.ts:11, 31`；没有 `android/`、`ios/` 资料夹
- **我会怎么做**：7 天不要碰。PWA + 加到主屏幕。任何「原生 App」句子从交件物删掉。

### [N20] `/api/ask` 是死路由；成本模型还在用它计价
- **严重度**：🟡 商业数字
- **在哪**：`src/` 没有 `fetch("/api/ask")`。章程问答是浏览器 `filterClauses`。`unit-economics.ts:220-226` 仍按每月 50 次 `/api/ask` 计价。
- **我会怎么做**：删掉对外引用。成本模型按 `/api/chat` + extract 重算。不要引用 73.4%，除非 `npm run check:ai` 证明路由真的在跑。

### [N21] 两套比赛叙事还同时活着
- **严重度**：🔴 对评审（混乱 = 不专业）
- **在哪**：产品是社团文件。`competition/r1-draft/03_CLAIM_LEDGER.md` 仍是「虚构社区礼堂旁坏掉的路灯」。`public/r1-demo/` 还留着 SVG。CLM-029 还写「代码没有向量库、没有 embeddings、没有 tool execution」——现在有 pgvector、`cari_minit`、function calling。
- **我会怎么做**：交件只用 Minit。路灯那一套整包封存，不要出现在 summary / disclosure / deck。AI 使用声明按 **现在的** 代码重写。

### [N22] Money 每一页右上角都有「全部删掉重来」
- **严重度**：🟠 对会抖的手
- **在哪**：`money-chrome.tsx:102-130`
- **我会怎么做**：移进设置，二次确认要打机构名。不要跟「开收据」住在同一条水平线上。

### [N23] 手机选单把一步一页摊成 19 个格子
- **严重度**：🟠
- **在哪**：`mobile-nav.tsx:72-92` + `sidebarPages()`
- **我会怎么做**：手机只留 4 项：主页、会议记录、钱、更多。一步一页的轨只出现在已经进入该件事之后。

### [N24] 正式产出是马来文，核对过程没有「这句马来文是什么意思」
- **严重度**：🟠 对 68 岁
- **在哪**：`minutes-document.tsx:94-97` 标题就是马来文会议记录
- **会发生什么**：她看不懂，不敢按保存。或者看不懂，乱按保存，错的进 eROSES。
- **我会怎么做**：每一段马来文下面一行中文对照（不是三语标签，是译文）。对照标明「这是给您看的，呈报用上面那行马来文」。

---

## 同意 prompt 5–9、不重述的

| 条 | 判决 |
|---|---|
| 5.1 四道关 | 同意基准。eslint 21 不是这次的问题。 |
| 5.2 空库 | **同意，我自己探过。** |
| 5.3 migration 16/17 | **同意，我自己探过。** |
| 5.4 印刷体假准确率 | 同意，并且被 N4 加重。我打开了 `eval/cases/case-01-minutes-agm/input.png`：900px 白底排版，不是手写。 |
| 5.5 function calling 没对真 API | 同意。形状测试 ≠ 厂商 200。 |
| 5.6 登入后没自动化 | 同意。我也没能登入。 |
| 5.7 八个证据点全空、没影片、没截图、没 Live URL、repo Private | 同意。commit 跨 2 天成立，但历史从 8/21 `clean tree` 起，评审会当成三天的黑客松。 |
| 5.8 价格互打 | 同意，且代码是第三套（见 N20）。 |
| 5.9 文件比程式腐、六棵树 | 同意。`STATE.md` 自己还留着已被推翻的「GitHub 凭证过期」。 |
| 6.3 反覆形状 | 同意，而且今天还在发生（N8 字号、N9 词库、N5 filings）。 |
| 8.13 全站聊天框不做 | **同意不做。** 现在已经有两个聊天入口（首页 AskBox + 浮动 dock），再做一个是灾难。 |
| 9.4–9.6 PDPA、作废、4 次呼叫扣 1 次 | 同意。4 次扣 1 次对庙是对的；对毛利要把最坏情况写进模型，不要当平均。 |
| 9.9 没做过使用者测试 | 同意。第 3 节的使用者画像来自 J 自己的社团经验，作为假说可以，当作研究不行。 |

### 第 8 节「已知还没做」该不该做（只排序，细节在 03）

| # | 7 天内？ | 理由 |
|---|---|---|
| 14 真实手写样本 | **必须** | 没有它 Technical 是空的 |
| 1 草稿／中途可走 | 必须（最小版：localStorage 已经在做，差的是告诉她「没保存也能回来」+ 不要 all-or-nothing） | |
| 2 历史改回去 | 交件后 | 现场 demo 不靠改历史 |
| 3 邀请第二人 | 交件后（见 N15） | |
| 4 Money 放 Excel | 不做 | 跟英雄流程无关 |
| 5 决议要整理 | **已经有** draft-minutes + coverage。要验的是手写，不是再写一条管线 |
| 6 AGM 缩写 | 7 天：从 demo 拿掉整页 |
| 7 词库让人乱 | 7 天：从主导航拿掉。首页抽取要接上词库（N9） |
| 8 Filings 位置 | 修 N5 比搬家重要 |
| 9 新帐号引导 | 必须（N10） |
| 10 订阅界面 | 不做。价格没定做界面是空的 |
| 11 总控台 | 不做 |
| 12 语音 | **不做。** 准确率零实证，4 天以上，会把仅剩的时间吃掉 |
| 15 eROSES 逐栏对 | 必须，人做，半天 |
| 16 法律 `[[ ]]` | 最小版（N16） |
| 17 roster 进呈报 | 必须（N13） |

---

## 做得对的（不准为了犀利而贬低）

这些是真的，不是礼貌。

1. **钱是 TypeScript。** 连号、缺口、交接状态机、RM1 万拆单、金额中文／马来文、预设不抵税。Hard Rule 2 在代码里成立，有测试。
2. **8/19 漏决议之后的反应是成人的。** `minutes-compose.ts` 的 coverage + 代码拼 Markdown，比「把 prompt 写得更凶」正确。
3. **paste-pack 拒绝把非 AGM/EGM 填进年报。** 这是在防假申报，不是功能缺失。
4. **审计行和抬头从 session 盖，不信浏览器。** `minutes/actions.ts` 那段注释记录的是真事故。
5. **工具不把捐款全名送给模型。** PDPA 做在查询上，不是做在「请你不要说出来」。
6. **BeforeReading、相机／档案拆开、出席 0 不算通过、FieldRow 的 choice/date。** 都是 J 撞过之后长出来的，方向对。
7. **`user-errors.ts` 的口气** 是全站最好的中文：不讲 MIME、教人怎么拍。问题是 `joinUserError` 把它又变回三语墙。
8. **示范不再一打开就灌进真流程**（`minutes-store.tsx:305-314`）。这一刀是对的。
9. **问题本身。** 90,224 个社团、义务秘书、混语手写、政府要马来文——这不是包装出来的 T5，这是马来西亚真实的公共治理缝。

内核配得上比赛。门、证据、口气，配不上。
