# 01 · 评分 · GROK · 2026-08-24

评审身分：Grok 4.6。程式只读。线上库只跑了只读探针 `npm run check:migrations`。没登入、没跑 eval、没碰 PPT。

**先把一句判断写在最上面。**

以 **2026-08-24 这一棵树、这一份线上库、这一个登入墙** 交出去，这不是冠军作品，也还不是能进 Semi-Final Demo Day 的作品。它现在是一套 **认真写成的合规内核**，外面套着 **评审进不去、老人用不了、核心卖点没证据** 的壳。

不该整栈砍掉重做。该砍的是 **比赛对外的那 3 分钟**，以及 **老人手上那条路**。栈、钱的 TypeScript、抽取 schema、coverage check，留着。

---

## 表 A · 比赛评审（MAIC Nexus Challenge 2026 · T5 · 一队 15 分钟）

评分口径：**交件当下评审能打开的东西**。不是「如果我把 49,532 行程式读完」。初赛问题写在官网：*"Is this a real attempt at solving a real problem with a real artifact?"*

| 项目 | 权重 | 实得分 | 理由 | 加 5 分最快的做法 |
|---|---|---|---|---|
| Technical Feasibility | 25 | **8** | 程式本身不是空壳：`receipts.ts` / `custody.ts` / `einvois.ts` 是真的 TypeScript；`minutes-compose.ts` 的 `checkCoverage` 是对着 8/19「17 条决议丢了 5 条」长出来的。648 个测试、`tsc` 0，这一层我信。**但 15 分钟评审看不到这些。** 他点开成品连结会撞上 `src/proxy.ts:38` 的登入墙；比赛规则写明有登入墙视为没交。线上库我今天自己探过：20 张表 **全部 0 笔**（`orgs` 也是 0）。核心卖点「读手写混语」量的是印刷体；更糟的是 `eval/reports/eval-2026-08-24T10-21-05.md` 在同一批印刷体上已经是 **92.9%、invented = 1**，对外还停在 95.2% / invented = 0。 | 做一个 **不用登入** 的 `/demo`：预填一个虚构社团、走完「拍照 → 核对 → 马来文会议记录」一次。不要用「请评审自己注册」。那仍然是登入墙。 |
| Commercial Viability | 25 | **6** | 市场数字是真的：JPPM 2026-07 的 90,224 个在册社团。付费理由也站得住（迟交 eROSES 会除名、义务秘书熬夜打马来文）。**商业模式这一格现在是自相矛盾的**，我同意 prompt 5.8，而且我在代码里又看到第三套：`src/lib/unit-economics.ts:162-166` 是 RM39/99/188 + HQ 150，`competition/business-model.md:69-74` 表格里 RM 200 那一行还在，`docs/方案与权益设计.md` 价格是空白 `__`。0 个客户、0 笔收入、20 个分堂是希望不是 LOI。一个 15 分钟评审问「谁付钱、付多少、你现在赚不赚钱」，三份文件三个答案，这一格直接沉。 | **今天定一个数字**。我会定 **RM 99 / 社团 / 月**，总会帐号 +RM 150。把另外两套从所有交件物里删掉。一句话：「还没有付费客户；毛利是模型不是发票。」 |
| Industry Relevance | 20 | **14** | 这一格是现在最强的。T5 要的是 civic tech + 多语 LLM + 政府数位化。eROSES 年度呈报、LHDN e-Invois、90,224 个没有专职秘书的社团，比「坏掉的路灯」更像马来西亚的公共治理。`paste-pack.ts:42-58` 拒绝把规划会议填进年报，这一刀是对的。扣分：`committee_roster` 写得进去、呈报读的是某次会议 AI 抠出来的 `office_bearers`（`paste-pack.ts:101-118`）；AGM 整页是示范（`SAMPLE_UNTIL_ROSTER_INGESTION = true`，`src/app/api/agm-pdf/route.ts:73`）；eROSES 栏位没对着真网站核对过。 | 用真 eROSES 网站截一张栏位名，跟 `src/prompts/eroses-map.ts` 逐格对一次。哪怕只对 8 格。把 AGM 页从对外 demo 拿掉。 |
| Scalability | 15 | **6** | HQ + 分会的树、RLS、`is_demo`、seed-demo 脚本，**纸上**能撑 20 个分堂。**实际上**新树 0 个 org；捐款登记簿活在 `minit:money:donations:v1`（`register-store.tsx:196-199`），换一台手机就没了；没有邀请第二个人的流程；没有 queue。这不是能横扫 9 万社团的系统，这是一台笔电上的工作副本。 | 跑 `npm run seed:demo`（虚构资料），让评审看到总会帐号能看见两个分会。同时把登记簿从资料库读回来，不要只写进去。 |
| ESG / National Impact | 15 | **7** | 故事是真的：义务、年长、华语脑子、马来文公文、捐款个资。遮名（`mask.ts`）、工具不选 `donor_name`（`org-tools.ts:33-35`）、收据预设「不是抵税」——这些是 ESG 该有的形状。**影响是 0。** `/privacy` 公开挂着 `[[nama entiti undang-undang]]`。画面上还写「目前请先用示范笔记，不要上传真实姓名」。一个给公共治理的产品，请使用者不要把真的公共资料放进来。 | 用 **4 张已同意的真实手写样本** 跑一次 eval，数字怎么难看就怎么写。ESG 这一格要的是诚实，不是 95%。 |
| **总分** | **100** | **41** | 问题是真的，内核是真的，成品不是真的。41 分在有公开 demo 的队伍里进不了前 30。 | — |

### 对照：如果评审拿到了 repo、愿意读程式

Technical 可以到 **15/25**，总分大约 **48**。仍然不是前三。差的不是「写得不够多」，是 **没有一个能打开的、诚实的、走得完的英雄流程**。

### 以现在这个状态交出去，会不会进初赛（2026 年 9 月）？

**不会。** 根据：

1. 官网成品规则：评审期间必须公开、无登入墙，否则视为没交。`src/proxy.ts:38` 的公开路径只有 `/login`、`/reset-password`、`/terms`、`/privacy`。`src/proxy.ts:10-15` 写明比赛 demo 已于 2026-08-21 删掉。
2. GitHub `https://github.com/ifelse3d/MINIT-PROJECT` 目前是 Private（公开抓是 404）。Repo 当作品也不合格。
3. 没有 Live URL。没有 `.vercel`。`capacitor.config.ts:31` 的地址是 `https://REPLACE-WITH-YOUR-URL/v2`，而 `/v2` 已经不存在。
4. 初赛筛的是「real artifact」。登入页 + 空库 + 印刷体准确率，不是 artifact。

Deck 写得再好也救不了这一条。**PPT 不是瓶颈，系统的门是瓶颈。**

### 一个评审能问出来、最伤的一个问题

> 「请现在打开你们的成品连结。不要登入。用一张手写的、中马英混着写的纸，让我看到政府要的那份马来文会议记录。」

今天的诚实答案是：连结没有；要登入；登入之后没有社团；没有手写证据；最新一次印刷体评测还发明了一条决议。

这个问题同时打穿 Technical、Industry、ESG。其他问题都是它的派生。

---

## 表 B · 普通民众（35 岁上班族，被推去当乡团秘书）

假设：会用 WhatsApp 和手机浏览器。没做过会议记录。没听过 eROSES。第一次自己打开这个系统。

| 项目 | 分数 | 理由（画面／档案） |
|---|---|---|
| 第一次打开，我知道要做什么吗 | **3** | 打开是登入页（`login/page.tsx:237-241`），标语是「社团与非政府组织的合规助手」——他不知道自己要合规什么。注册成功后被丢到 `/constitution?setup=1`（`create-org-form.tsx:19`），他只是想来做会议记录。首页「一扇门」在没有机构时是锁的（`ask-box.tsx:262-331`）。底下却挂着三条 LHDN e-Invois 死线（`page.tsx:72` + `standard-deadlines.ts:59-60`，没有 AGM 也照样算 3 条），看起来像已经欠税。 |
| 我看得懂画面上的字吗 | **5** | 文案本身很多是白话，`user-errors.ts` 甚至会教他怎么拍。预设却是 BM · 中 · EN 三语串一行（`language-provider.tsx:16, 80-83`）。右上角有三颗语言按钮，但没有人告诉他「关掉两个」。错误讯息 `joinUserError` 永远三语全印（`user-errors.ts:27-28`），关掉语言也没用。 |
| 我找得到我要的功能吗 | **4** | 他要的是「做会议记录」和「开收据」。侧栏把这两件事拆成 4+5 个步骤名（`nav-items.ts:86-96`），手机汉堡是 **约 19 个入口的两栏网格**（`mobile-nav.tsx:72-92`）。同一件事三个名字：首页「会议笔记」、组名「会议记录」、第一步「拍照与核对」。Money 更乱：首页「捐款与收据」、侧栏「登记与收据」、分页「开收据」（`money-chrome.tsx:23-28` vs `nav-items.ts:93`）。 |
| 出错的时候我知道怎么救吗 | **6** | 这一项相对好。HEIC、档案太大、AI 挂了，`user-errors.ts` 会讲下一步。坏的是：真的校验失败时仍可能落到 `serverError`（「是 Minit 这边出了问题，请等一分钟」`user-errors.ts:125-128`）。收据开到一半失败时叫他「不要再按、先去历史」——这对没做过的人是惊吓，不是指引。 |
| 我敢不敢把真的捐款资料放进去 | **2** | 画面自己说不要上传真名。开了收据就不能删（`register-store.tsx:379-384`），没有作废／重开。登记簿在这台手机的 localStorage，换手机就没了，历史页却在资料库。WhatsApp 文案写着「PDF 已附上」（`receipts.ts:299`）但 `wa.me` 附不了档案。我不会把真的善款放进去。 |
| 我会不会推荐给我的社团 | **3** | 问题他认得出（「就是我们每年在熬的那个」）。产品他不敢推荐：自己还没走完一次，而且看起来随时会把资料吞在某一台手机里。他会说「再观察」。 |
| 手机上能用吗 | **4** | 相机／选档案已经拆开（`143fb42`，这是对的）。触控主按钮有 `min-h-11`。但分页轨 `min-h-9`（`section-tabs.tsx:73`）不到 44px；打字输入捐款是 `min-w-[40rem]` 的表（`type-donations.tsx`）；玻璃 + sticky 分页 + 搜索 + 汉堡 + 右下角 AI 圆钮，6 吋屏幕能做事的区域是一条缝。 |

**这一角平均约 3.9 / 10。** 一个 35 岁秘书 **可以被教会**，前提是有人坐在旁边点一次。他一个人，第一次，会卡在注册和「我该按哪一个」。

---

## 表 C · 68 岁、小学程度、庙里的财政（6 吋 Android、识中文、马来文只认得几个词）

| 项目 | 分数 | 理由 |
|---|---|---|
| 字看得清楚吗 | **3** | 预设字级 112.5%（约 18px，`appearance-provider.tsx:42-47`）。最大一档 140%＝22.4px，对 68 岁读中文仍然偏小。更致命：标题字体是 Inter、`subsets: ["latin"]`（`layout.tsx:14-18`），`html lang="ms"` 写死（`layout.tsx:72`），全站字族没有 Noto Sans SC / 微软雅黑。中文在马来文 Android 上会落到系统后备，又小又挤（`letter-spacing: -0.01em`）。字级设定藏在登入之后的设置页，登入页没有。 |
| 按钮按得到吗（手指粗、手会抖） | **5** | 主按钮够大。会按错的是：语言小芯片、分页轨、右上角 Money 每一页都有的「全部删掉重来」（`money-chrome.tsx:102-130`）、交接页上「我交出」和「总会确认」两颗同时出现。 |
| 看得懂中文的部分吗（不识马来文） | **3** | 中文文案经常是好的（「交现金」「读账页」）。她 **看不见纯中文**，因为预设三种全开，侧栏 `truncate` 切成 `拍照与核对 · Phot...`（`sidebar.tsx:92`）。`joinUserError` 无视语言偏好。正式会议记录是马来文——产品就是这样设计的——但她要对一份自己读不懂的公文按「保存」。条款页是 BM+EN，注册却要她勾同意。 |
| 「拍照 → 核对 → 存起来」我自己走得完吗 | **2** | 这条路实际是：电邮+复杂密码（大写+小写+数字+符号，`login/glass.tsx:36-46`）→ 可能还要去邮箱点确认（`login/page.tsx:200-207` 写明这个专案 **Confirm email = ON**）→ 建机构 → 被带到章程 → 回首页发现拍照按钮是灰的 → 找到「拍照与核对」→ 先填「你本来就知道什么」→ AI 读 → **每一格**按「没错／修改／笔记里没写」→ 分页写「2」进去标题写「3」（`attendance-review.tsx:145`）→ 再进一页标题写「4」和「5」→ 有一格没按完就不能看正式文件（`minutes-document.tsx:83-88, 287`）→ 保存。她走不完。 |
| 哪一步会让我不敢按，怕按坏 | — | **「开收据」**（号码不能回头）。**「全部删掉重来」**。**「用 WhatsApp 发送」**（她以为发出去了，捐款人没收到 PDF）。**绿色的「没错」**（她不确定按了会不会改到真的帐）。任何写着 eROSES / e-Invois / LHDN 的按钮。 |
| 哪一步会让我放弃，去叫孙子来 | — | **注册那一页**（电邮和「要有符号」）。过了之后是 **核对页**：20 个栏位 × 3 个按钮，她以为自己在填政府表。再不行就是 **马来文那份文件**——她看不懂，不敢按保存。 |

**这一角是三个身分里最低的。** 产品自称的使用者，正好是这一角。

---

## 三个角度里哪一个最低？应该先救哪一个？

**最低：68 岁财政。** 平均大约 3 / 10。产品是写给她的，界面是写给写程式的人的。

**但 7 天内要拿第一，先救的不是她，是比赛评审那一角的门。**

原因很冷：68 岁那一角再修 3 天，没有公开成品连结，初赛仍然当没交。门打开之后，**立刻**把 68 岁那条路变成 demo 上唯一的路——因为 10 月现场 Demo Day，评审就是用手机在看。一个进得去、老人走得完的英雄流程，同时救 A 和 C。B 会跟着好。

具体就是一件事：

**一个不用登入的、中文预设的、两步走完的「拍照 → 马来文会议记录」公开 demo。**

不是再做一个词库，不是再做一个助手工具，不是重写 Next.js。

---

## 我对第 7 节那 7 条的判决（评分用，细节在 02）

| 条 | 成不成立 | 一句话 |
|---|---|---|
| 7.1 migration 16/17 | **成立。今天我自己探到 NOT YET。** | 工具 8/24 修好了，库还没跑。日历／交接／分组在别的手机上是幽灵。 |
| 7.2 三语变 `...` | **成立。而且是根，不是皮。** | 预设三种全开 + `truncate`，不是排版事故，是语言模型错了。 |
| 7.3 为什么叫「拍照与核对」 | **成立。名字起错。** | 系统在说自己的工序。她要的是「做会议记录」。 |
| 7.4 排版奇怪 | **成立。拆页拆对了病、开错了药。** | 按工程模块拆成 9 个网址，不是按她脑子里的一件事。 |
| 7.5 一笔一张卡片 | **成立。200 笔会崩。** | 旁边其实有 `type-donations.tsx`，她找不到；找到了也是 640px 宽的表。 |
| 7.6 号码对不上、被流程绑 | **成立。** | 分页自动编号、区块手写编号，两套。被锁的下一步连结在 `NextStepLink` 里直接消失（`page-section.tsx:78-83`），跟 CLAUDE.md 第 13 条自己写的相反。 |
| 7.7 不人性化 | **成立。这是总症。** | 系统在用审计员的口气跟义工说话。每一页都是关卡。 |

---

## 我不同意 prompt 的地方（评分有关的）

1. **`draft-minutes.ts` 零 importer 已经过时。** 现在有 `/api/draft-minutes`，`minutes-store.tsx:732` 会叫它。08-19 的「写完没接上」那一条，这一条已经接上了。
2. **「上传前小表单还没做」已经过时。** `before-reading.tsx` 在，commit `58c1447`。`docs/产品缺口盘点.md` 08-23 更新区块自己已经落后一天。
3. **分组「仍未做」已经过时。** 程式有 `groups-card.tsx` + migration 17；缺的是跑 migration，不是写程式。
4. **对外 95.2% / invented = 0 不只是「量的是印刷体」。** 2026-08-24 同一批印刷体已经量到 **invented = 1**。核心军规在自己的黄金样本上破了。prompt 5.4 没写到这一刀。
5. **不该为了贴 T5 去走路灯故事。** `competition/r1-draft/03_CLAIM_LEDGER.md` 还在描述一个已删除的「社区报修路灯」demo。那是假的智慧城市。真的 T5 故事就是 90,224 个社团的法定呈报。删掉的 r1-demo 不要救。
6. **「AI 不算钱 / AI 不准编」这两条规矩本身是对的，不要推翻。** 要推翻的是第三条规矩的执行方式：三语标签强制同时出现。以及「eROSES 测试」被执行成「每一格都要人按一次」——那是把空表换成确认表，力气仍然从人流向系统。
