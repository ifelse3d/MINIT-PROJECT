# =============================================================================
# minit-v2 · .env.local 范本
# 2026-08-20 · 每一行的模型都是当天用 J 自己的 key 实际问过 API 确认存在的，
#              而且都在 src/lib/ai/gemini.ts / openai.ts 的价格表里。
#
# 🔴 价格表以外的模型 = cost_micros 会是 null = 管理台的「本月真实成本」那一格坏掉。
#    要换成表外的模型，先去那两个档案加一行价格。
#
# 填法：把 <> 里的换成真的值。这个档 .gitignore 挡着，永远不要 commit。
# =============================================================================


# --- Supabase（新 project）---------------------------------------------------
# ⚠ 建 project 之前先决定 region，它要写进隐私权告知，而且建下去改不了。
SUPABASE_URL=<https://xxxx.supabase.co>
SUPABASE_SERVICE_ROLE_KEY=<eyJ... service_role，绕过 RLS，绝不可外流>
NEXT_PUBLIC_SUPABASE_URL=<跟 SUPABASE_URL 一样>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<eyJ... anon，不是 service_role>


# --- AI keys -----------------------------------------------------------------
# 🔴 GEMINI_API_KEY 一定要用「付费 Tier 1」那一把。
#    免费 tier Google 会拿输入去训练（.env.example 里那段警告），PDPA 上不能用。
#    J 2026-08-20 在 AI Studio 确认过现在这把是 Tier 1（Spend 页有真实费用）。
GEMINI_API_KEY=<AIza...>
OPENAI_API_KEY=<sk-proj-...>

# GPT-5.6 系列上，detail 不给或给 auto 会被当成 "original"，
# 一张 3000x4000 的照片 = 11,750 patches 的输入（openai.ts 档头的注解）。
# 所以这一行是刻意压着成本的，不要拿掉。
OPENAI_IMAGE_DETAIL=high


# --- 一个任务一个模型 ---------------------------------------------------------
# 格式一律 provider:model。少了 "provider:" 不会报错，会被当成 model 名字然后打错厂商。

# 手写、混合语言的那一支。全 app 最难的一格。
# 🔴 不要动。对外讲的 95.2% 是在这一支上量的，换掉数字就作废，
#    而且现在没有真实手写样本可以重量。
# ⚠️ 下面这四支是「现在在跑的」，不是「量过最好的」。
#    2026-08-20 J：「不要一直只死拿着之前那个 95.2%，不要把偏见保留下来。」
#    95.2% 只量过准确率，没量成本、没量速度、没跟任何模型比过。
#    → 先照抄让新树跑得起来，跑完 `npm run bench` 再回来改这四行。
#    → 之后这四格会搬进资料库（ai_model_config），在 /admin 里改，不用重新部署。
#    完整理由：docs/模型评测与总控台.md
AI_MODEL_EXTRACT=gemini:gemini-3.5-flash-lite

# 30 页章程。先维持不动，等有真实章程再验。
AI_MODEL_LONG_DOC=gemini:gemini-3.5-flash-lite

# 「这张是会议记录还是帐本？」三选一。全场最便宜（US$0.05 / 0.4 每百万 token），
# 而且它只做一件很简单的事 —— 这一格 nano 就够，不用换。
AI_MODEL_CLASSIFY=openai:gpt-5-nano

# 🔴 助手。这一格要换掉 gpt-5-nano。
# 重做后的助手要自己决定「该用哪支工具、要不要再查一次」——
# nano 做得到分类，多轮工具选择会掉链子，那正是「智障」的来源。
# gpt-5.6-luna: US$0.2 / 1.2 每百万 token —— 比 gpt-5-mini（0.25 / 2.0）还便宜，
# 而且世代新很多。换完要重跑 npm run economics，成本地板会动。
AI_MODEL_CHAT=openai:gpt-5.6-luna

# 🆕 会议记录的语意搜寻（「我记得有一次开会说了什么」）。
# 程式码还没有这两个变数，助手重做第 2 步会加。
# gemini-embedding-001 预设吐 3072 维 —— 🔴 pgvector 的 hnsw 索引上限是 2000 维，
# 3072 建不了索引。它支援 outputDimensionality 截短，实测 768 和 1536 都能要到。
# 选 768：安全、索引快、储存小、品质损失很小。
# ⚠ 这个数字会写死进 20260822000000_minutes_search.sql 的 vector(768)。
#   之后要改维度 = 重建索引 ＋ 重算全部 embedding。现在就想清楚。
AI_MODEL_EMBED=gemini:gemini-embedding-001
AI_EMBED_DIM=768


# =============================================================================
# 🔴 旧的 .env.local 有、但新的「不要带过去」
# =============================================================================
#
#   AI_PROVIDER=gemini
#       上一代的东西。现在四个 slot 各自写 provider:model，这个变数早就没人读了。
#       留着只会让人以为改它有用，然后改了半天没反应。
#
#   GEMINI_MODEL / OPENAI_MODEL
#       同上（旧的 .env.local 没有，但 .env.example 里还印着，顺手一起清掉）。
#
# =============================================================================
# 实测纪录（2026-08-20，用 J 自己的 key 打 API 问出来的，不是从文件抄的）
# =============================================================================
#
#   Gemini 这把 key 看得到的 embedding 模型只有三支：
#       gemini-embedding-001 · gemini-embedding-2-preview · gemini-embedding-2
#   三支预设都是 3072 维。
#   ❌ text-embedding-004 不存在 —— 我 8/20 稍早推荐过它，那是错的，已更正。
#
#   outputDimensionality 实测：
#       gemini-embedding-001  ask 768  → 拿到 768   ✅
#       gemini-embedding-001  ask 1536 → 拿到 1536  ✅
#       openai text-embedding-3-small  → 1536       ✅（备案）
#
#   价格表里认得的模型（表外的 cost_micros 会是 null）：
#       gemini: 3.1-flash-lite 0.25/1.5 · 3.5-flash-lite 0.3/2.5 · 3-flash 0.5/3.0
#               3.5-flash 1.5/9.0 · 3.6-flash 0.75/3.75 · 3.7-flash 0.75/3.75
#       openai: gpt-5-nano 0.05/0.4 · gpt-5.4-nano 0.2/1.25 · gpt-5-mini 0.25/2.0
#               gpt-5.6-luna 0.2/1.2 · gpt-5.4-mini 0.75/4.5 · gpt-4.1-nano 0.1/0.4
#               gpt-4.1-mini 0.4/1.6 · gpt-5.6-terra 2.0/12.0 · gpt-5.6-sol 5.0/30.0
#
#   ⚠ gemini-3.7-flash-lite 不存在（价目表里那句写对了）。
#     但 gemini-3.6-flash 存在而且跟 3.7-flash 同价，之前的文件没提到。
