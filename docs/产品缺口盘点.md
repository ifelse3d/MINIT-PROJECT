# 产品缺口盘点 — J 2026-08-20 走一次系统的 23 条

> **固定档名，下次盘点覆盖它**（`CLAUDE.md` 文件规则）。
> **这一份取代 `docs/UX-问题清单.md`（2026-08-07 的 20 条版本）。**
> 我没有自作主张删掉那一份 —— 它从来没进过 git，删了就找不回来。
> 你确认之后再把它移进 `docs/archive/`。那 20 条的下场收在第 6 节，一条都没丢。

**写于 2026-08-20 · 距竞赛截止 2026-08-31 23:59 MYT 还有 11 天**

## 怎么读这份文件

| 标记 | 意思 |
|---|---|
| **[查过]** | 我打开那个档案、那一行看到的。附档名与行号，你可以自己去看 |
| **[实测]** | 向真的 Supabase 问过（唯读，service_role）。附日期 |
| **[人眼]** | 没有 API 回报得了，只有人看得到的 |
| **[推断]** | 我从前两者推出来的，**还没验证**。要当成假设，不是事实 |

**这份文件回答**：使用者眼中哪里不对 · 程式码上到底是什么状况 · 要动哪些档案 · 多贵 · 什么风险。
**不回答**：先做哪一个 —— 那是 J 的决定，见第 5、7 节。

---

## 0. 一分钟版本

1. **23 条里，有 4 条（5、6、10 的一半、7 的一半）是同一个 bug。** 一行 schema。见第 1 节。
2. **有 3 条其实已经做好了或做了一半**（13 全做好、21 一半、20 一半躺在工作树里没 commit）。
3. **真正是新工程的，剩 16 条。** 加起来 **19–25 个专注工作日**，而剩 11 天。算术在第 7 节。
4. **2026-08-07 那两个「根」今天还在**：不能自己打字加一行（根 A）· 18 张表有 12 张是空的（根 B，8/20 实测）。你这次的第 3、4、9、10、13 条全部长在这两个根上。
5. **有 5 件事我不能替你决定**（法律实体、真实资料、定价、注册开放、eROSES 谁来核对）。见第 5 节。

---

## 1. 一个根，三个症状 —— 今天最重要的发现

**你昨天填的两个值，画面收下了，伺服器两边都拒绝。**

你在第 2 步「Fill in／自己填写」里填了：

- 会议类型 → `event meeting`
- 日期 → `2/2/2026`

但程式里这两个栏位不是自由文字：

| 在哪 | 写着什么 |
|---|---|
| `src/lib/extraction.ts:90` **[查过]** | `MEETING_TYPES = ["agm", "egm", "committee"]`，schema 是 `z.enum([...MEETING_TYPES, ""])` |
| `src/lib/extraction.ts` `dateFieldSchema` **[查过]** | `/^\d{4}-\d{2}-\d{2}$/` —— 只收 `YYYY-MM-DD` |
| `supabase/migrations/20260708000000_init.sql:92` **[查过]** | `meeting_type text not null check (meeting_type in ('agm','egm','committee'))` —— 资料库这里还有一条 |

而第 2 步的输入框是 `src/app/minutes/minutes-review.tsx:67` 的 `FieldRow`，**所有栏位共用同一个纯文字 `<input>`**（第 1026–1092 行三个栏位都是它）。它不知道自己正在编辑一个 enum 或一个日期。

于是：

| 你按的 | 程式里发生什么 | 你看到的 |
|---|---|---|
| 让 Minit 写正式文件 | `src/app/api/draft-minutes/route.ts` 开头 `parseMeetingNotesExtraction()` 失败 → 回 400 `USER_ERRORS.serverError` **[查过]** | 「Ada masalah di pihak Minit / 是 Minit 这边出了问题」← **第 5 条** |
| Save to History | `src/app/minutes/actions.ts:87` 同一个 parse 失败 **[查过]** | 「Data minit tidak lengkap / 会议记录资料不完整」← **第 6 条** |
| 看 `/filings` | `src/lib/paste-pack.ts:42` 找不到对照就原样印出 **[查过]** | eROSES「Jenis Mesyuarat」= `event meeting`，还标着 Confirmed ← **第 7 条的一半** |
| 看 History | 那份记录从来没存进 `minutes_docs` | 只看得到上传记录 ← **第 10 条的一半** |

### 这不只是一个 bug，是两个更深的问题

**a. 错误讯息把使用者的输入问题，讲成了系统的问题。**
「是 Minit 这边出了问题」是 `USER_ERRORS.serverError`，它的注解写着「Anything unexpected on the server. Never leak the cause.」**[查过]** 对真正的伺服器故障这是对的；对「你填的值不在允许范围内」这是错的 —— 那是使用者救得回来的事，而讯息叫他去等一分钟。
**教训：拒绝一个输入，要在拒绝的那一刻讲清楚哪一格、为什么、怎么改。**（这跟 8/19 汇入失败给出路是同一条原则，只是没有套到这里。）

**b. `MEETING_TYPES` 只有三种，是 eROSES 的紧身衣，不是社团的现实。**
你 2026-08-18 讲过：「社团不只是做会议给 eROSES，我要做到的是团体里面都可以自己使用好用的。」
`STATE.md` 已经把这句话记下来了，也写着「要加会议类型，但那会牵动 eROSES mapping，要另外评估 —— 刻意还没动」。**这次你就是从使用者的位置撞上了那个「刻意还没动」。**

### 修它要动什么（提案，未执行）

| 档案 | 改什么 |
|---|---|
| `src/lib/extraction.ts` | 类型放宽到 6 种（agm/egm/committee/planning/event/other）＋ `meeting_type_label`（社团自己的叫法，选 other 时才填）＋ `EROSES_FILEABLE_MEETING_TYPES = ["agm","egm"]` |
| `minutes-review.tsx` | `FieldRow` 加 `inputKind`：类型 → 下拉（含「其他，自己填」）、日期 → 日期选择器（永远吐 `YYYY-MM-DD`）、标签改成 Meeting date／Meeting venue |
| `minutes-lang.ts`／`minutes-draft.ts`／`minutes/history/page.tsx` | 三语标签补上新类型 |
| `paste-pack.ts` | **非 AGM／EGM 的会议：印「这场会议不进年报」，不是把 `planning` 塞进政府栏位** |
| `minutes/actions.ts` | `normaliseMeetingType` 跟着放宽 |
| **一支 migration** | 资料库那条 check 也要放宽。**已经写好草稿**：`supabase/migrations/20260820000000_meeting_types_and_minutes_draft.sql`（**未批准、未执行，没有任何东西指向它**） |

**代价**：约 0.5 天 ＋ 你贴一次 migration。
**一次修掉**：第 2、5、6 条，第 7 条的一半，第 10 条的一半。

> ⚠️ **一个必须写下来的取舍：不要顺手改 `src/prompts/extract-meeting-notes.ts`。**
> 那支 prompt 现在只让模型回 `agm|egm|committee|""`。一旦放宽它，**对外讲的 95.2% 准确率当场作废**（`STATE.md` 第 6 节：「换掉一个预设值，等于让所有量测过的东西悄悄失效」），而 11 天内不可能重量。
> **新类型只给「人」在第 2 步选，AI 的合约一个字不动。** 效果一样拿得到 —— 你自己选比 AI 猜准 —— 代价是零。
> 那支草稿 migration 里也把这段理由写进注解了。

---

## 2. 两个旧的根，今天还在

### 根 A · 「你 → 记录」这条路径不存在

2026-08-07 就写下来了，今天一个字都不用改：整个核对画面（`minutes-review.tsx`／`money-review.tsx`）的资料来源**只有 AI 抽取的结果**。栏位可以「修改」，但**不能新增一行、不能删除、不能从零打一份**。

你这次的第 **3**（出席要自己勾人）、**4**（决议要整理／补充）、**9**（草稿）、**8**（Money 放自己的 Excel）全部长在这个根上。

> 「直接自己打字／加一行」是整份清单里 CP 值最高的一件：**不需要 AI、不烧额度、不需要付费 key。**

### 根 B · 18 张表，12 张是空的 —— 2026-08-20 实测

**[实测 2026-08-20]** 用 service_role 向 `ivsqiycvahcevzgokgvc` 逐张问过（唯读）：

| 有资料 | 笔数 | | 空的 | |
|---|---|---|---|---|
| `orgs` | 4 | | `paste_packs` · `committee_roster` · `expenses` | 0 |
| `uploads` | 6 | | `remittance_batches` · `einvois_packs` · `constitutions` | 0 |
| `members_roles` | 6 | | `events_meetings` · `deadlines` · `qa_log` | 0 |
| `donations` | 10 | | `extractions` · `reminders` · `rsvps` | 0 |
| `receipts` | 10 | | | |
| `ai_usage` | 14 | | | |
| `org_glossary` | 1 | ← 你昨天加的「新民班」 | | |
| **`minutes_docs`** | **1** | ← **只有一份。所以 History 看起来是空的** | | |

**这解释了你的第 10、11、13 条，而且比「没做好」更精确：**

- **第 10 条**：`/history` 其实**有**读 `minutes_docs`（`src/db/activity.ts:44` **[查过]**）。它看起来只有上传记录，是因为 `minutes_docs` 真的只有 1 笔、`uploads` 有 6 笔。**不是页面漏做，是东西没存进去** —— 而东西存不进去，就是第 1 节那个 bug。
- **第 13 条**：`committee_roster` **0 笔**。名单页做好了（见第 3 节第 13 条），只是还没有人被加进去。
- **第 11 条**：`members_roles` 有 6 笔，所以「谁能登入」是真的在资料库里 —— 缺的只有「怎么邀请第二个人进来」。

---

## 3. 逐项 23 条

每一条的格式：**你说的** → **查到的事实** → **要动什么** → **代价** → **风险／先决条件**。
代价单位是「专注工作日」（我写程式、你测；不含你贴 migration 与来回确认的时间）。

---

### 1 · 放照片就直接走了，没机会说这是什么会议 🔴

**你说的**：放照片下去他还是直接走，想 type 跟他说这是什么会议没办法；有时照片里写的是**活动时间不是会议时间**；人员安排那些也要处理好。

**查到的**
- `minutes-review.tsx:899` **[查过]** `accept="image/*"` —— **连 PDF 都不收**。（`/money` 那边是 `image/*,application/pdf`，`money-review.tsx:918`，两边不一致）
- 选了档案就直接呼叫 AI，**没有确认视窗、没有先看图、没有取消的机会**。`STATE.md` 第 3 节已经把这一条列为「知道要做，还没做」的第 2 项。
- 「活动时间 vs 会议时间」：extract prompt 里 `meeting_date` 只说「会议日期」，白板上写的活动日期长得一模一样，模型分不出来 —— **这是设计缺口，不是模型不行**。
- 「人员安排」：`office_bearers` **是法定申报栏位**（映射 eROSES「Senarai Ahli Jawatankuasa」）。一次性的活动分工塞进去 **= 不实呈报**。`STATE.md` 第 6 节写得很清楚。所以「人员安排」需要的是**另一个栏位**，不是塞进 office_bearers。

**要动**：第 1 步加一个上传前的小表单（会议类型／日期／地点由人先填，AI 就不必猜）＋ 确认视窗（几页、会用掉多少额度）＋ `accept` 加 PDF ＋ extraction 加一个 `duty_assignments`（活动分工，明写不进 eROSES）。

**代价**：0.5 天（不含 `duty_assignments`）／＋0.5 天（含）
**风险**：`duty_assignments` 会动 extraction schema ＋ prompt → **动 prompt 就要重跑 eval**。建议这一半留到竞赛后；先只做「人自己填会议基本资料」。

---

### 2 · 会议类型／日期／地点 🔴 **根**

**你说的**：meeting type 可以给 user 选或者自己 type 比较好。Date 写「MEETING DATE」比较不会混乱，可以点选还是写，不过要放好是 Y/M/D，不然每个人格式不同。Venue 也是「MEETING VENUE」。

**查到的**：**见第 1 节。** 你这一条不是体验改善 —— 它是第 5、6 条失败的根，也是第 7 条印出非法值的原因。你自己指出的三件事（可选、日期格式、标签讲清楚）**恰好就是修法**。

**要动 / 代价 / 风险**：见第 1 节的表。0.5 天 ＋ 一支 migration。

---

### 3 · 出席名单没有要求 🟠

**你说的**：一定要要求，不然都不知道谁开会。之后要能选群体（AJK、青年、小天使），可单选可多选，社团自己建 category，选了之后出名单可以 tick。照片或 PDF 里有的就 AUTO。

**查到的**
- 现在出席可以整个空着就过关。你的截图里「Who attended ✓ All checked (0)」—— **0 个人也算「全部核对完」**。
- `committee_roster` 表**存在且有 RLS**（Phase 7），8/19 加了 `name_official` 栏，但 **[实测] 0 笔**。
- 「群体分类」目前**零程式码、零资料表**。要新增一张 `member_groups`（或在 `committee_roster` 加 `group` 栏）＋ 使用者自建分类的 UI。
- 「照片里有的自动带进来」：extract prompt 8/18 才刚改成「出席名单只收明确的出席标题」（防止乱塞），所以自动带进来的东西**本来就会偏少** —— 这是刻意的。

**要动**（分两段，可以只做前段）
- **前段**：出席不能是 0（除非明确勾「这场没有记出席」）＋ 从 `committee_roster` 勾人 ＋ 手动加人 ＋ AI 读到的自动打勾。**0.75 天。**
- **后段**：使用者自建群体分类、单选多选、分类出名单。**＋1.25 天 ＋ 一支 migration。**

**风险**：出席人数会进 eROSES「Bilangan Ahli Hadir」。`/filings` 现在那一格写着「由系统从已确认的出席名单算，永远不是 AI 算的」**[查过]** —— **所以出席一旦能填，那一格就活了**。这是好事，但代表这一条同时影响申报，要一起测。

---

### 4 · 决议要 AI 整理，不只是抠字 🟠

**你说的**：希望由 AI 辅助，不只是抠字，还要整理好、写好。AI 要看这是什么，然后整理好，不然做出的 report 也是没办法使用，不然 AI 也是白放。

**查到的 —— 这一条你看到的画面是误导的**
- `src/app/api/draft-minutes/route.ts` **[查过]** 是 2026-08-19 才建的，它**真的会呼叫模型**，而且做的正是你要的：让模型**分组＋措辞**、回传编号，**程式再验证每个编号恰好出现一次**（`checkCoverage`）、每一串中文都必须逐字出现在来源或词库里（`checkNames`）。
- **但你昨天看到的不是它的输出。** 它在第一行 parse 就 400 了（第 1 节），画面退回 `renderMinutesDraftBm()` 的**模板** —— 把确认过的字串照原文重印，套上写死的马来文标题。**你说的「只是抠字」，说的就是那个 fallback。**
- 为什么架构是「模型只做分组，程式做算数」：8/19 第一版让模型直接写整份文件，它交回排版良好的 BM 分节，**17 条里少了 5 条（家长班整段），输出里没有任何迹象**。`STATE.md` 第 6 节：「模型会安静地漏掉，而且漏掉的时候输出看起来最漂亮。」

**要动**：**先修第 2 条，再实拍一次，看它真正写出来的样子。** 那之后才知道要不要改 prompt。可以预期还要调的：分组的标题、`TUJUAN MESYUARAT` 那段（现在是程式串出来的，因为让模型自由写会捏造 —— 8/19 它生出「露营、家庭日」，白板上两样都没有）。

**代价**：实拍验证 0.25 天；真要调分组与措辞 ＋0.75 天。
**风险**：🔴 **不要为了「写得更漂亮」把覆盖率检查放宽。** 那道检查是唯一挡住「安静漏掉」的东西。

---

### 5 · 「正式会议报告」变成这样 🔴 **＝ 第 1 节的 bug**

**你说的**：这里也是有问题，也不正式，不是有叫说 AI 做出正式会议报告吗？现在又变成这样。

**查到的**：**同第 4 条。** 你按的那颗按钮回了 400，画面退到模板，所以看起来「不正式」。这不是设计倒退，是那次呼叫根本没成功。

**代价**：0（含在第 2 条里）

---

### 6 · Save 有问题 🔴 **＝ 第 1 节的 bug**

**你说的**：然后 save 也是有问题。

**查到的**：`src/app/minutes/actions.ts:87` **[查过]**，同一个 parse。**代价**：0（含在第 2 条）。

**额外要修的一件小事**：那句「Data minit tidak lengkap — 请重新载入页面再试」是**错的建议** —— 重新载入不会有帮助，因为坏的是你填的值。**讯息要指到那一格。**

---

### 7 · eROSES 的值不能用 🔴🔴 **两个问题叠在一起**

**你说的**：value to eROSES 也是不行，因为要做好可以适配 eROSES 的，这也是需要 AI 的地方。

**查到的 —— 这一条比你想的严重，而且第二半跟 AI 没关系**

**(a) 印出非法值**：`paste-pack.ts:42` **[查过]** 找不到对照就原样印，所以 `event meeting` 被当成 Jenis Mesyuarat 还标 Confirmed。修法见第 1 节。

**(b) 🔴 整包粘贴包的来源是错的。** `STATE.md` 第 6 节已经查证过：
- `/filings` 的粘贴包是 `buildPastePack(extraction)` 建的 —— 来源是**某一次会议记录里 AI 读出来的 `office_bearers`**，而且是从 **localStorage 的 `minit.minutes.v1`** 读的。
- 整条路上**没有 `name_official` 这个概念**，也**从来不碰 `committee_roster`**。
- 换句话说：你在 `/members` 辛苦维护的理事名单（含 IC 上的名字），**跟你要贴进政府网站的那一包完全无关**。

**这不是「加一个 if」，是换掉 eROSES mapping 的来源。**

**(c) 🔴 没有人拿真实的 eROSES 网站逐栏核对过。** 画面上那句「送出前请先跟正式网站核对，ROS 的栏位名称会变」就是在承认这件事。**这一格错了，后果是政府表格上的不实资料。**

**要动**：(a) 0.25 天 ／ (b) 1.5 天（改来源、接 `committee_roster`、缺 `name_official` 时挡住申报 —— 你 8/19 已经决定「卡在申报，不卡在加人」，这就是那个「没有地方可以挂」的地方） ／ (c) **不是程式，是你或懂 eROSES 的人打开真的网站逐栏对一次。**

**风险**：⚠️ 「用 AI 适配 eROSES」这个方向要小心。**eROSES 的栏位是固定的、法定的，那是查表不是判断。** 让模型去猜哪个值该填哪一格，正好是它会安静出错的地方，而这一格出错的名字叫「不实呈报」。**建议：mapping 用程式写死＋人核对，AI 只用来把社团的用词整理成候选值，最后一步永远是人按确认。**

---

### 8 · Money 不能放文件 🟠

**你说的**：money 这里为什么不能放文件，我这里有做好的 Excel 也不能放。

**查到的**：`money-review.tsx:918` **[查过]** `accept="image/*,application/pdf"` —— **收照片和 PDF，不收 Excel／CSV**。

**好消息是这条路已经写好了，只是在别的页面。** `/members` 8/19 做了「汇入分两条路」：Excel／CSV **由程式解析、免费、不花 AI 额度**；照片／PDF 走 AI（1 次额度）。还有可下载的 Excel 范本（`/api/list-template`）。**整套搬过来即可。**

**要动**：`/money` 汇入面板做成两颗大按钮（价钱写在按钮上）＋ 账本范本 ＋ 复用 `read-roster` 那条「AI 只填进框、人按了才写资料库」的规矩。

**代价**：0.5 天
**风险**：⚠️ **金额一律程式解析，不要送 AI。** Hard Rule 2：所有加总是 TypeScript。而且捐款人姓名送去厂商是 Hard Rule 5 的问题。

---

### 9 · 没有 Save as draft，也没地方看回去改 🟠

**你说的**：全部也没有 SAVE AS DRAFT 的 button，也没有一个地方可以看回去之前，可能要做修改还是什么都没办法。

**查到的**
- `npm run status` **[查过]**：`④ Save as draft` = **[ 没做 ]**。
- **一半的零件已经有了**：`src/lib/minutes-draft.ts` 的 `DRAFT_WATERMARK`；而且 `20260708000000_init.sql:96` **[查过]** `status text not null default 'draft' check (status in ('draft','confirmed'))` —— **资料库早就允许 draft 了**，不用为它改 schema。
- **真正缺的是**：要能「点回去继续核对」，就必须把当时那份 extraction 一起存下来 —— 它现在只活在 component state ＋ localStorage 的 `minit.minutes.v1`。

**要动**：`minutes_docs` 加 `extraction jsonb`（只有 draft 会填）＋ 存草稿的 action ＋ 历史页点得回去 ＋ 草稿没有审计行（Hard Rule 8：那一行只属于人确认过的文件）。三个地方要：会议记录第 2／3 步、收据、托管批次。

**代价**：1.5 天（含第 10 条）＋ 一支 migration
**风险**：🔴 **草稿存资料库，不要存 localStorage。** `usePersistentState` 的 key 不绑成员 —— 同一台电脑的另一个成员会看到你的草稿。这正是 champion Phase 4 修掉的那一类缺陷。

---

### 10 · History 只看到 upload record 🟡 **一半是第 1 节的 bug**

**你说的**：history 只有可以看到 update record，没有之前的做出来的 record。

**查到的**
- `/history` **有**读会议记录：`src/db/activity.ts:44` **[查过]** `.from("minutes_docs")`，另外还读 receipts／donations／expenses／remittance／einvois／paste_packs／uploads／constitutions／qa_log／deadlines／events。**页面没有漏做。**
- **[实测 2026-08-20]** `minutes_docs` = **1 笔**、`uploads` = **6 笔**、其余大多 0。**所以画面上看起来就是「只有上传记录」。**
- 另外还有一个**入口问题**（2026-08-07 就记过）：`/minutes/history` 这一页**存在**，但左侧栏没有它，只有 `/history`。而 `/minutes/history` 也**没有任何筛选** —— 没有日期范围、没有会议类型。

**要动**：(a) 修第 1 节的 bug，东西才存得进去；(b) 会议记录页面上直接给「过去的记录」入口；(c) 加筛选（日期／类型）。

**代价**：0（a 含在第 2 条）＋ 0.5 天（b、c，含在第 9 条的 1.5 天里）

---

### 11 · 「Who can use Minit」没做好 🟠

**你说的**：who can use minit 这个也是还没做好。

**查到的**：`/members` 底下那句「Inviting other people is not built yet (P1-1)」是**真的**。`npm run status`：`⑤ 邀请成员 P1-1` = **[ 没做 ]**。`members_roles` **[实测]** 有 6 笔，所以「谁能登入」的资料结构是活的，缺的是「怎么把第二个人拉进同一个 org」。

**要动**：`invites` 表（`org_id`／token／role／`expires_at`／`used_at`）＋ 接受邀请的页面 ＋ 接上 Supabase auth ＋ 完成后写一行 `members_roles`。**建议做邀请连结**（WhatsApp 传过去，点了自己设密码）—— 管理员代开帐号会让密码被写在纸上；加入码会被转传。

**代价**：1.5 天 ＋ 一支 migration
**先决条件**：🔴 **P0-3（`createOrg` 数量上限）必须先做。** 现在一个帐号可以开无限个 org，每个 org 每月 100 次免费 AI —— 开放给别人注册之前不补，一个人用键盘就能无限印额度。而且 Vercel 上线那份文件要你把 `Confirm email` 设成 OFF，两件事叠在一起就是一扇开着的门。

---

### 12 · Committee 汇入的两条路讲不清楚 🟢 便宜

**你说的**：EXCEL/CSV 没用 AI 的要备注好是要跟着 TEMPLATE 的；photo/PDF 那边也是要放没跟 TEMPLATE 的 Excel，因为有些社团有做好的没跟着 TEMPLATE 的，那就没办法 UPLOAD 如果没有得选。

**查到的**：你说得对，而且这是一个**真实的死路**。现在 Excel／CSV 那条走程式解析，**看不懂的行会整批拒绝并列出行号**（这是对的设计），但一份不跟范本的 Excel 从头到尾都不会被接受，而画面上没有讲「那你可以走另一条」。8/19 做的「汇入失败给出路」按钮是从**文字**那条路救的，档案这条没接上。

**要动**：Excel／CSV 那颗按钮写明「**要跟范本**（这里下载）· 程式读的，免费」；照片／PDF 那颗改成「**任何格式都行**，包括你自己做的 Excel · 会用掉 1 次 AI 额度」，并让它真的收 `.xlsx`（现在只收图片／PDF）。

**代价**：0.25 天
**风险**：⚠️ 试算表摊平成文字时**空格是位置不是没有** —— 8/19 踩过：`[崇德][空][ajaran]` 被读成「把崇德翻译成 ajaran」，规则被安静改掉。有 Tab 的行要保留栏位位置。

---

### 13 · Committee 没地方看有谁 ✅ **已经做好了**

**你说的**：committee 这里也是没有地方看有谁，需要弄成有 COMMITTEE LIST，然后也有 UPLOAD COMMITTEE 的地方。

**查到的**：**两个都已经有了。** `/members` 上半张卡就是「Committee list · 理事名单」（`src/app/members/page.tsx`，8/19 `e0a55d8`＋`d95aba3` 做的），「Already have a list? Bring it in all at once」就在同一张卡里，含 Excel 范本下载。你自己的截图里也看得到。

**[实测]** `committee_roster` = **0 笔** —— 所以它是空的，不是不存在。

**要动**：0。**要做的是把人填进去。**（顺带：这也是 `/filings` 那格「Senarai Ahli Jawatankuasa」为什么是 `—` 的原因 —— 但接上它是第 7(b) 条的工作。）

**代价**：0

---

### 14 · Filings 要写明确，位置可能不对 🟢 便宜

**你说的**：Filing 这里要写得明确，可能不该放在 DOCUMENT 这里，要写好这是应该在 EROSES 的做什么用的，这样 USER 才知道不用去猜。

**查到的**：`/filings` 其实**是左侧栏的一级项目**（`nav-items.ts` **[查过]**：Filings 排在 Money 后面），不在 Documents 底下 —— 你看到的「Documents ▸ Filings」是**次级选单又列了一次**。**同一个东西出现在两个层级，本身就是让人猜的原因。**

**要动**：(a) 二选一，不要两边都放；(b) 页面顶上一句话讲清楚：这一页产生的是**呈报社团注册局（ROS）年报（Penyata Tahunan）时，eROSES 网站上要一格一格填的值**；(c) 每一格旁边写它对应 eROSES 的哪一栏、什么时候要交（AGM 后 60 天）。

**代价**：0.25 天

---

### 15 · AGM 页要写明确、不要缩写 🟠

**你说的**：AGM 也是写明确，有很多人不懂 SHORTFORM，然后里面的确保都是 EROSES 需要的，不要等下少这个那个。

**查到的 —— 这里有一件你还不知道的事**
- 「不要用缩写」是对的：AGM＝Mesyuarat Agung Tahunan＝常年大会，第一次出现要写全名。**便宜，0.25 天。**
- 🔴 **但 `/agm-pack` 整页现在是示范资料。** `src/app/api/agm-pdf/route.ts` 里 `SAMPLE_UNTIL_ROSTER_INGESTION = true` **写死**，每一页盖「CONTOH — JANGAN GUNA（示范，请勿使用）」，资料来自 `sample-roster.ts`。**[实测]** `committee_roster` 0 笔也印证了这件事。
- 所以「确保都是 eROSES 需要的」这句话，在这一页目前**没有意义** —— 它还不是你的资料。

**要动**：(a) 文案去缩写化 0.25 天；(b) **让 AGM 包吃真实的 `committee_roster`** —— 跟第 7(b) 条是同一件工作，1.5 天里包含。

**风险**：`api/agm-pdf` 里那段「there is no committee-roster ingestion」的注解**已经过期**（8/19 起真的有了），改的时候要一起改，否则下一个人会照着它做错判断。

---

### 16 · Constitution ⏸ 等你补充

**你说的**：CONSTITUTION 这里是 OK，只是等下跟你说我还要的事情。

**查到的**：**[实测]** `constitutions` = **0 笔** —— 页面在等你拍第一页。写入端 8/5 已经补上（11 张空表里唯一被救回来的一张），所以拍下去会真的存进资料库。
**已知的一个缺口**（2026-08-07 记过，今天仍然成立）：抽出来的结构化条文躺在 `constitutions` 表里**没有阅读介面** —— 只能问问题，不能像看条文一样一条一条读。

**代价**：等你讲完再估。

---

### 17 · Our Words 让人混乱 🟢 便宜，但影响很大

**你说的**：有点混乱，你要给 EXAMPLE，然后什么意思是 KEEP AS WRITTEN、什么意思是 write it as，不要做到人混乱；然后记录好的就放在下面，如果记录一大堆，要再加得一直滑下去很麻烦。

**查到的**：你的截图就是证据 —— 你把「新民班」填进「The word」，选了「Write it as…」，然后把「Kelas Xin Min」填进了**「What is it?（这是什么）」**那一格，「Written as」是空的。**表单让你填错了，而它没有挡。** 后来那一笔存成了「kept as written / KELAS XIN MIN」，跟你想要的不一样。

这一格**很重要**，因为词库会接进 extract prompt 和写文件的 prompt —— **填错的词库会安静地改变输出**。

**要动**
- 两个选项各配一个**实例**，就写在选项旁边：
  - **保持原字**：`余老师` → 文件里永远写「余老师」。**人名、职称、教理名词选这个。**
  - **写成…**：`新民班` → 文件里写「Kelas Xin Min」。**社团的班别、活动名要在马来文文件里出现另一种写法时选这个。**
- 选了「写成…」而「Written as」是空的 → **挡下来**，不要存。
- 已经记录的收进一个可折叠、可搜寻的清单，新增表单永远在最上面（现在是清单在上、表单在下，多几十笔就要一直滑）。
- 🔴 加一句红字：**「身份证上的名字（`name_official`）不可以放进词库。」** 那不是翻译，是另一个法定事实，音译一个填进政府表格＝不实呈报。

**代价**：0.25 天

---

### 18 · 要当作真正使用，不再是 DEMO 🔴 **这是一个决定，不是一个功能**

**你说的**：我要自从最开始使用，这将会是真正使用，用我的社团来测试。我也会给其他人做测试。所以不再是 DEMO 这样，所以我要先前面的都弄好处理好。
**（后来补充）**：我要当作我是全新用户，第一次使用的感觉，而不是透过你帮我处理好。

**技术上挡你的已经没有了**：Gemini 2026-08-20 确认在 **Tier 1 付费层**，免费层那句「Used to improve our products: Yes」不再适用 → 真实资料可以进系统。这曾经是整份 `STATE.md` 里最大的一条阻塞。

**还挡着的是三件不是程式的事：**

| | 是什么 | 谁能解 |
|---|---|---|
| **法律实体** | `legal/` 两份文件的 `[[ ]]` 还没填，而且**控制者是谁还没决定**（你个人 vs Sdn Bhd）。用个人名义营运一个装着他人敏感个资的系统，**责任无限** | 只有你 |
| **PDPA 同意** | 会员／捐款人的资料要进系统，需要书面告知与同意。庙宇捐款名册**算不算敏感个资**是律师判断题 | 你＋律师 |
| **注册那扇门** | 上线要求 `Confirm email = OFF`（评审要能五秒进来）＋ `createOrg` 没有上限（P0-3）＝ 一个人可以无限印额度 | 程式（0.5 天） |

**关于「全新用户的感觉」—— 这一句我认为是你今天讲的最有价值的一句，但有一个前提要先分清楚**

- 「按下去会失败」不是体验问题，**是一面墙**。撞墙的人只学到「这里过不去」，学不到别的。你昨天已经撞过那面墙（第 5、6 条）。
- **建议：先只拆墙，不布置房间。** 只修第 2 条（半天），其他一律不碰、你的资料一笔不动。然后用**全新浏览器 profile ＋ 没用过的 email ＋ 全新社团 ＋ 你自己真的一张白板**，我不在旁边，你从头走到最后，每一次「咦？」记一行。
- **那份「咦？」清单会比这 23 条更准**，因为它按你**真的撞到的顺序**排，不是按想起来的顺序。你昨天那五张截图就是一次小规模的实验，它挖出了一个根、一次解释四条。

**代价**：拆墙 0.5 天（＝第 2 条）；P0-3 0.5 天；法律那两条是你的时间，不是我的。

---

### 19 · 新账号要有引导，要问新社团还是旧社团 🟠

**你说的**：关于新账号想要你做好 GUIDE，教怎样使用，然后也要问是新社团还是已经成立久的社团，因为旧社团可能有自己的 constitution，可以 upload 然后资料都会比较齐全那些。当然它也可以之后才 upload，还是他要做新的。

**查到的**：现在 `/login` 注册完就直接进首页，**零引导**。`/orgs/new` 只问机构名字。「新社团 vs 旧社团」这个分岔**完全没有** —— 但它其实是个好设计：旧社团有章程、有理事名单、有往年会议记录；新社团什么都没有，需要的是范本。

**要动**（分两段）
- **前段**：注册后一个三问的开场（新／旧社团 → 有没有章程 → 有没有理事名单），按答案给不同的下一步。**0.5 天。**
- **后段**：完整的分支引导（旧社团：上传章程 → 汇入理事 → 拍第一份记录；新社团：章程范本、理事职位范本）。**＋0.5 天。**

**风险**：⚠️ **引导做完要自己走一次。** 8/19 踩过两次：词库做好只放在设定页（「没看到有 /glossary」）、新页面用 `max-w-2xl` 而全站是 `max-w-5xl`（「为什么那么窄」）。**写完文案要在画面上读一次。**

---

### 20 · 改成进来有 chatbox 🔴 **最大的一件**

**你说的**：想要改成进来有 CHATBOX 可以聊天的，这个改动是很大，不过我要弄一个很好的交互体验，所以我决定改。然后 TOKEN 用 percentage 计算。然后在每个有使用 AI 的地方都有得看自己的 USAGE 剩多少，有一个 BAR 和 percentage 可以看。

**查到的 —— 三件事，难度差很多**

| | 状态 |
|---|---|
| **AI 面板本身** | 有，而且**改写版已经写好躺在工作树里没 commit**：`src/components/v2/ai-dock.tsx`（未追踪）＋ `app-shell.tsx`＋`mobile-nav.tsx` 的改动。从「盖住画面的抽屉」改成「会把页面推开的可调宽度侧栏」。**tsc／测试／lint／build 都是连它一起跑绿的** |
| **用量百分比** | **一半做好了**：`src/app/settings/ai-usage-card.tsx` **[查过]** 已经在算 `pct`。缺的是抽成共用元件，放到每一个会花额度的按钮旁边 |
| **全站 chat-first** | **零。** 这是把每一个页面的进入方式换掉 |

**要动 / 代价**
- **薄片（建议）**：commit 那个侧栏 ＋ 首页让它主动开口 ＋ 用量 bar 到处可见。**0.5 天。**
- **完整版**：全站改成对话优先。**3–5 天，而且它会碰每一个页面** —— 在一个「按 Save 会失败」还没修完的产品上做这件事，风险最高。

**风险**：🔴 **首页那个对话现在只活在 component state** —— 问完 → 去别页 → 回首页，对话就不见了。要做持久化**不要顺手用 `usePersistentState`**（key 不绑成员，同一台电脑的别人看得到）。

---

### 21 · 订阅界面 🔴 **卡在还没定的商业决策**

**你说的**：订阅界面那些都没有，这些都是要已经有的。然后也可以知道自己是什么配套。Setting 里有 USAGE 可以看还剩多少。

**查到的**
- **[查过]** 全 repo grep `subscription|Langganan|billing|plan tier` → **零个结果**。没有 plan、没有 seat、没有付款、没有 trial。
- **[查过]** `settings/ai-usage-card.tsx` 已经有「本月 AI 操作 N / 100 免费」＋ 加购额度 ＋ 绿／琥珀／红徽章。**「Setting 里看还剩多少」这一件其实已经有了**，只是没有 bar 和百分比。
- 🔴 **`STATE.md` 第 5 节第 12 题还没答**：方案价格、各方案席位数、分会数上限都没定案，而且 `business-model.md` 里还有一张 RM200 的过期价格表跟 deck 的 RM39/99/188 对不上。**毛利模型（73.4%）是建在 RM39/99/188 上的。**

**要动**
- **薄片**：用量 bar ＋ 百分比到处可见 ＋ 设定页一张唯读的「你的方案」卡。**0.25 天。**
- **完整版**：方案选择、席位、加购、付款流程。**2 天（不含金流串接）＋ 你先把价格定了。** 金流（Stripe／Billplz）再 ＋3 天以上。

**风险**：定价一动，`unit-economics.ts`、deck、one-pager 都要跟着重算。**不要手抄数字进任何文件，一律跑 `npm run economics`。**

---

### 22 · 我自己的总控台 🟠

**你说的**：我自己也需要一个类似总控台，之前有讨论过，类似管理台，好像每个公司都有一个管理账户的，不然也不知道要怎样看有什么问题，或者反应的 bug 那些。这个我是真不懂。

**查到的**
- **[查过]** 没有 `src/app/admin`。零。
- 有一份旧计划：`docs/审查请求-health上锁与总控台.md`（未追踪，从来没进过 git）。
- 🔴 **一个你会撞到的设计限制**：`service_role` **被刻意挡在 `minit_admin.*` 外面**（`STATE.md` 第 6 节）。加额度只有一条路：`select * from minit_admin.grant_ai_credits(<org id>, <量>);`。**所以未来的管理后台不能直接呼叫它** —— 要另外设计一条有审计的路。
- 顺带：`/health` 现在**任何登入的使用者都进得去**，而且会列出 `SUPABASE_SERVICE_ROLE_KEY` 等**变数名称**（值有遮，名称本身就泄漏架构）。这是 D2，还没决定。

**要动**
- **薄片（建议）**：一页**唯读**的 `/admin`，只给你（hardcode 你的 user id 或一张 `platform_admins` 表）：几个 org、各自本月用量与成本、最近的错误、最近的 ai_usage。**1 天。**
- **完整版**：加额度、看别人的资料、处理回报的 bug。**2–3 天 ＋ 一套权限设计 ＋ 一支 migration**，而且「看别人的资料」有 PDPA 后果，要先想清楚边界。

**风险**：🔴 **管理台是全系统权限最大的一页。** 它做错的后果比其它 22 条加起来都大。**不建议在 11 天冲刺里做完整版。**

---

### 23 · 语音（新增，J 2026-08-20 追加）🟠

**你说的**：还有一个放语音录音还是说话的这个也要做。**（后来确认）**：两个都要，A 先。

**查到的**：**[查过]** 零行程式码。2026-08-07 的盘点也是零。

**这是两件不同的事，工量差 4 倍：**

| | A · 用讲的代替打字 | B · 整场录音 → 会议记录 |
|---|---|---|
| 使用者做什么 | 栏位旁边按麦克风，讲「二零二六年二月二日」 | 手机放桌上录两小时 |
| 技术 | 浏览器内建语音辨识（Web Speech API） | 长音档上传 → 转写 → 抽取 → 核对 |
| 花不花额度 | **不花** | 按分钟计费，是目前最贵的一格 |
| 准确率 | 讲错就改，风险低 | **完全未知，没有任何 eval** |
| 代价 | **1 天** | **4 天以上** |

**A 要注意的**：Web Speech API 在 Chrome 可用、Firefox 不行，要看得出来「你的浏览器不支援」；马来文／华语／英语混讲的辨识率不高，所以**结果一定要进「人确认」的框**，不能直接存。

**B 要注意的**：🔴 **录音是一份新的敏感资料**（会议里有人名、有金额、有争议）。它同时触发第 18 条的三个法律前置。而且「会议录音转记录」的准确率**必须先量**才能对外讲 —— 不能重蹈「95.2% 量的是印刷体不是手写」那个覆辙。

**建议**：A 做进这几天；**B 写进 9 月的计划，不在这 11 天**。

---

## 4. 依赖图 —— 哪几件不能跳过顺序

```
   ┌─ 2（会议类型／日期／地点）  ← 一切的根
   │   ├─→ 5（正式文件）   ← 直接解掉
   │   ├─→ 6（Save）      ← 直接解掉
   │   ├─→ 7a（eROSES 不印非法值）
   │   ├─→ 10a（东西存得进去，History 才有东西）
   │   └─→ 4（要先能成功呼叫，才看得到 AI 真正写的样子）
   │
   ├─ migration 20260820（你贴）
   │   ├─→ 2 的资料库那一半
   │   └─→ 9（Save as draft 的 extraction 栏位）→ 10b（点回去继续）
   │
   ├─ committee_roster 有资料（你填 or 汇入）
   │   ├─→ 3b（从名单勾出席）
   │   ├─→ 7b（eROSES 粘贴包改从名单来）
   │   └─→ 15b（AGM 包吃真实资料，拿掉 CONTOH 浮水印）
   │
   ├─ P0-3（createOrg 上限）
   │   └─→ 11（邀请成员）→ 18（开放给别人测试）
   │
   └─ 价格定案（你决定）
       └─→ 21b（订阅界面）
```

**三个卡在你身上、不卡在程式的**：贴 migration · 填 `committee_roster` · 定价格。
**一个卡在律师／你身上的**：第 18 条的法律实体与 PDPA 同意。

---

## 5. 要 J 决定的（AI 不该自己选）

| # | 决定什么 | 不决定的后果 |
|---|---|---|
| **D-a** | **法律实体是谁**（个人／Sdn Bhd）。`legal/` 要填控制者名字与地址 | 第 18 条不能真的开始。用个人名义装他人敏感个资，责任无限 |
| **D-b** | **真实资料现在放不放**（全放／只放知情理事／先用假资料） | 影响截图能不能用、影响 PDPA 风险 |
| **D-c** | **方案价格与席位数**（`STATE.md` §5 第 12 题，7/29 留到今天） | 第 21 条完整版做不了；`business-model.md` 那张 RM200 的过期表继续跟 deck 打架 |
| **D-d** | **注册开放还是邀请制**（D3，一直没答） | 不管选哪个 **P0-3 都要先做**；开放的话还要处理 `Confirm email = OFF` |
| **D-e** | **谁去拿真的 eROSES 网站逐栏核对**（第 7c 条） | 粘贴包正确与否目前**没有人验证过**，而这一格错了叫不实呈报 |
| **D-f** | 第 16 条你还没讲完的部分 | — |

**已经答过、不要再问的**（`STATE.md` §5 已记）：`name_official` 缺了卡在申报不卡在加人 · 第 3 步用方案 A · 词库两个接点一次做完 · 报告三语 · 日常跑 master。

---

## 6. 2026-08-07 那 20 条的下场（一条都没丢）

| 旧编号 | 内容 | 2026-08-20 的状态 |
|---|---|---|
| 19 | AI 面板桌面版不能打字 | ⚠️ **修好了但没 commit** —— `ai-dock.tsx` 躺在工作树里。见新第 20 条 |
| 15、20 | 深色模式看不清 | ✅ **已修** `85c04b1`（每个对比度数字都重算过）。**截图还没拍** |
| 8 后半 | `/filings` 有复制按钮、`/minutes` 没有 | ❓ **未复查**。同一个 `lib/paste-pack.ts`，两个画面待遇不同 |
| 3 | Word／Excel／打字／录音收不收 | 🟡 部分：Excel 汇入名单 8/19 做了；**minutes 连 PDF 都还不收**（新第 1 条）；打字仍然没有（根 A）；录音＝新第 23 条 |
| 5、9 | 不能自己加一行 | 🔴 **仍然没做**（根 A）→ 新第 3、4、8 条 |
| 4 | 一百个出席者会很长 | 🔴 仍然没做 → 并进新第 3 条 |
| 6 | 职位跟章程连动 | 🔴 仍然没做（`committee_roster` 8/19 有表有页面，**[实测] 0 笔**）→ 新第 7b、15b |
| 7 | 找回旧记录难 | 🔴 仍然：`/minutes/history` 在，但没筛选、侧栏没入口 → 新第 10 条 |
| 8 前半 | eROSES 对不对得上 | 🔴 **仍然没有人核对过** → 新第 7c 条，D-e |
| 10、11、12 | 托管／日历／History 空的 | 🔴 **[实测] 仍然 0 笔**（根 B） |
| 13 | 章程只能看 PDF | 🟡 抽取有、问答有、**条文阅读介面仍然没有** → 新第 16 条 |
| 14 | 「上传记录」为什么在那 | ⏸ D4，未决 |
| 16 | AI 不要用次数 | ⏸ **D1 未决**；「显示成百分比」不需要等资料 → 新第 20、21 条 |
| 17 | `/health` 谁看得到 | ⏸ **D2 未决** → 并进新第 22 条 |
| 18 | 注册开放？订阅方案？ | 🔴 **仍然零**（D3 未决 ＋ P0-3 未做）→ 新第 11、18、21 条 |
| 1、2 | 侧边很多选项 | — |

**看得出一个模式**：8/07 到 8/20 之间，**被修掉的都是「一个 class」「一个色码」那种**；**两个根（A：不能自己打字 · B：11 张空表）一个都没动过**，而你这次的 23 条里有 5 条直接长在上面。

---

## 7. 时间的算术

### 7.1 加起来是多少

| 组 | 项目 | 天 |
|---|---|---|
| **便宜** | 2＋5＋6（会议基本资料，一次修四条） | 0.5 |
| | 7a（eROSES 不印非法值） | 0.25 |
| | 1 前段（上传前先填 ＋ 吃 PDF） | 0.5 |
| | 8（Money 吃 Excel／CSV） | 0.5 |
| | 12＋14＋17（汇入选路、Filings、Our Words） | 0.75 |
| | 15a（AGM 去缩写化） | 0.25 |
| | 20 薄片＋21 薄片（AI 侧栏 ＋ 用量 bar） | 0.5 |
| | 13 | **0（已完成）** |
| | **小计** | **3.25** |
| **中** | 9＋10（Save as draft ＋ 历史点得回去） | 1.5 |
| | 3 前段（出席必填 ＋ 从名单勾） | 0.75 |
| | 4（决议整理，先实拍再调） | 1.0 |
| | 23A（语音口述） | 1.0 |
| | 19 前段（开场三问） | 0.5 |
| | P0-3（createOrg 上限） | 0.5 |
| | 22 薄片（唯读 `/admin`） | 1.0 |
| | **小计** | **6.25** |
| **贵** | 7b＋15b（eROSES／AGM 改吃 `committee_roster`） | 1.5 |
| | 11（邀请成员） | 1.5 |
| | 3 后段（自建群体分类） | 1.25 |
| | 1 后段（活动分工栏位，要动 prompt ＋ 重跑 eval） | 0.5＋eval |
| | 19 后段（完整分支引导） | 0.5 |
| | 20 完整（全站 chat-first） | 3–5 |
| | 21 完整（订阅界面，**要先定价**） | 2＋ |
| | 22 完整（管理台） | 2–3 |
| | 23B（整场录音 → 记录） | 4＋ |
| | **小计** | **16.75–19.75** |
| | **合计** | **26–29 天** |

**这还没算**：你贴 migration 的等待、来回确认、你测出来的新问题、竞赛那边（上线／截图／访谈／eval／8-31 交件核对）。

**剩下的**：11 天。你说要用 **2 天**。

### 7.2 「2 天全部做完」与「不要偷工减料」不能同时成立

这两句话是你同一段话里讲的，我两句都当真，所以必须讲清楚它们在打架：

- **26–29 天的工作压进 2 天 = 每一件做十四分之一。** 那就是偷工减料，只是分散在 23 个地方，看起来像「全部都做了」。
- 而且**半成品比没做更糟**：一个只做一半的出席名单会让人以为出席已经记录了；一个只做一半的 eROSES mapping 会让人把错的值贴进政府网站。**第 1 节那个 bug 本身就是这个道理的示范** —— 画面允许的比后端允许的多，中间那一截没做完，结果是「Minit 这边出了问题」。

**所以我不会说「2 天可以」然后交 23 个半成品给你。** 我会照下面的形状做，做完的每一件都是真的做完的，没赶上的我会指名讲。

### 7.3 三个形状

| | **形状 A · 2 天** | **形状 B · 5–6 天** | **形状 C · 11 天全押产品** |
|---|---|---|---|
| 做完什么 | 便宜组（第 1、2、5、6、7a、8、12、14、15a、17、20 薄片、21 薄片）少一两项 | ＋ 中组（9、10、3 前段、4、23A、19 前段、P0-3、22 薄片） | ＋ 贵组一半（7b、15b、11、3 后段） |
| 你拿到什么 | **主流程从头到尾走得通、存得住、eROSES 不再印非法值** | **一个真的能交给社团用的产品**：草稿、历史、出席、语音、引导、你的管理台 | ＋ 第二个人能进同一个 org、eROSES／AGM 吃真实名单 |
| 20（全站 chat）／21（订阅）／22（完整管理台）／23B（录音） | 薄片 | 薄片 | 薄片 |
| 竞赛那边 | 还来得及（剩 9 天） | 还来得及（剩 5–6 天） | **放弃**：证据维持 0/8，自评 37/100 |

> **形状 A 的另一个价值**：做完它，你就能照第 18 条那样**用全新帐号从头走一次**，而且不会撞墙。那份「咦？」清单会直接决定形状 B 的顺序 —— 花 2 天换一个比这份文件更准的排序，是划算的。

**我的建议是 A → 走一遍 → B。** 但这是你的产品，你说了算。

---

## 8. 明写「不做」的东西，以及为什么

写下来是为了三天后没有人以为它被漏掉了。

| 不做 | 为什么 |
|---|---|
| **改 `src/prompts/extract-meeting-notes.ts`** | 一改，对外讲的 95.2% 当场作废，11 天内重量不了。新会议类型只给人选 |
| **20 全站 chat-first** | 3–5 天，碰每一个页面。在主流程刚修好的产品上做这个，风险最高 |
| **21 完整订阅＋金流** | 价格与席位还没定案（D-c）。做出来是空壳，而且定价一动要重算毛利与 deck |
| **22 完整管理台** | 全系统权限最大的一页，而且 `service_role` 被刻意挡在 `minit_admin` 外，要另设一条有审计的路 |
| **23B 整场录音 → 记录** | 第二条产品线。新的敏感资料类型 ＋ 准确率零实证 |
| **3 的自建群体分类** | 要一支 migration ＋ 一套 UI。前段（必填＋勾名单）先拿到八成价值 |
| **1 的「活动分工」栏位** | 会动 extraction schema ＋ prompt → 要重跑 eval。而且塞进 `office_bearers` = 不实呈报，必须另开栏位 |
| **D1（额度单位从「次」换成成本）** | 要两周真实资料，8/18 才开始收。**不要提前改 `unit-economics.ts` 或 deck** |

---

## 9. 验收 —— 每一项怎么算「做完」

**规矩：每一条都要有一个「能跑的指令」或「一个画面上看得到的结果」，不能只有一句「做好了」。**
（`STATE.md` 第 6 节：一个「有公式」的数字不等于一个诚实的数字；一份写完没执行的审查等于没做过。）

| # | 通过的样子 |
|---|---|
| **2·5·6** | 拿**同一张**那面白板重走：类型从下拉选「活动策划会议」→ 日期用日期选择器 → 按「让 Minit 写正式文件」**不再红字** → 按 Save **真的存进 `minutes_docs`**（用 PostgREST 数一次，从 1 变 2） |
| **7a** | 同一份记录去 `/filings`：Jenis Mesyuarat 那格印的是「这场会议不进年报」，不是 `event meeting` |
| **1** | 上传前的表单填了之后，第 2 步那三格已经是你填的值，AI 不再覆盖它 · 丢一份 PDF 进 `/minutes` 收得下 |
| **8** | 拿你**真的那份 Excel** 丢进 `/money`，画面明写「程式读的，免费」，读出来的行进的是可编辑的框，按了才写资料库 |
| **9·10** | 核对到一半按「存草稿」→ 关掉浏览器 → 从历史点回去**接着核对**，而且草稿上**没有审计行**、有 DRAFT 浮水印 |
| **3** | 出席 0 人不给过（除非明确勾「这场没记出席」）· 从理事名单勾得到人 · `/filings` 的「Bilangan Ahli Hadir」跟着出现数字 |
| **4** | 同一张白板的 25 条决议，**一条都不能少**（`checkCoverage` 会挡）· 输出里没有白板上没有的字（`checkNames`）· 三种语言各出一次 |
| **12·14·17** | 拿一份**不跟范本**的 Excel 汇入理事，能走通 · Our Words 选了「写成…」而没填就存不了 |
| **20·21 薄片** | 每一颗会花额度的按钮旁边都看得到 bar ＋ % · 首页对话换页再回来**还在** |
| **22 薄片** | `/admin` 只有你进得去（换一个帐号试）· 看得到 4 个 org 的用量 |
| **23A** | 讲一句话，字进框里，**而且仍然要人按确认才存** · 用不支援的浏览器打开时看得出来 |
| **全部** | `tsc` **0** · `lint` **27 problems**（＝既有基准，不是新做坏的）· **39 档 568 测试全绿**（新行为要有新测试）· `build` 成功 |

---

## 10. 现在的状态（2026-08-20 写这份文件的当下）

- **程式码：一个字都没改。** 2026-08-20 我曾误读一句话开始动 `extraction.ts`／`minutes-lang.ts`／`salin-migration.bat`，**已经 `git checkout` 全部还原**，`git status` 确认过。
- **唯一留在磁碟上的新东西**：`supabase/migrations/20260820000000_meeting_types_and_minutes_draft.sql` —— **提案，未批准、未执行，没有任何东西指向它**（`salin-migration.bat` 还是指向 8/19 那两支）。它做两件事：放宽 `meeting_type` 的 check ＋ 加 `extraction jsonb`（第 9 条要用）。**两件合成一支，你只需要贴一次。**
- **工作树里原本就有、我没碰的**：`app-shell.tsx`／`mobile-nav.tsx`（M）＋ `ai-dock.tsx`（未追踪），以及 `r1c-output/` 那批 8/12 的旧产出。

---

*下一步：你选一个形状（第 7.3 节），或者回答第 5 节那几个决定。在那之前不动任何程式码。*
