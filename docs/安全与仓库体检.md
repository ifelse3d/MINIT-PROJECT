# 安全与仓库体检 — 2026-08-20

> **固定档名，下次体检覆盖它。**
> 这一份回答 J 2026-08-20 晚上问的四件事：95.2% 还算不算数 · 要不要开新 repo ·
> 资料／AI／其他安全 · 「你确定都做好了吗」。
>
> **每一条都附「怎么查到的」。** 没查到的我写「查不了」，不写「应该没问题」。

| 标记 | 意思 |
|---|---|
| **[实查]** | 我在 J 的机器上跑了指令看到的，附指令 |
| **[实测]** | 向真的 Supabase 问过（唯读，service_role），2026-08-20 |
| **[读码]** | 我打开档案看到的，附行号 |
| **[查不了]** | 从外面问不出来，需要 J 在 Supabase 跑一句唯读 SQL |

---

## 0. 一分钟版本

1. **95.2% 已经不描述今天的系统了 —— 而且不是今天弄坏的。** 那支 prompt 在 **8/18 被加了 19 行**（`d1142ff`），量测是 **8/06**。今天这支 commit **没有碰 prompt、没有碰模型层**（`git show --stat` 证实）。所以：eval 必须重跑，但那是 8/18 就欠下的债，不是今天新欠的。
2. **不要开新 repo。乱的是「产出资料夹」，不是程式码。** 1,959 个追踪档里 **1,458 个（74%）是 `r1c-output/`** 的旧审查产出。真正的产品是 `src/` 226 个档。**删掉那几个资料夹，评审看到的就是一个乾净的 repo** —— 半小时的事，不用重写任何东西。
3. **安全最好的一块是资料隔离，最差的一块是「写好了但没套到资料库」。** RLS 设计是扎实的（16 张表 × 4 条 policy，用 `accessible_orgs()`）；**但 `20260730000000_receipt_series` 到今天还没套用** [实测]，而 `receipts` 已经有 10 笔资料。
4. **git 历史里没有任何真的密钥。** [实查] 全历史扫过 `AIza…／sk-…／eyJ…／AQ.…`，只找到 `mock-supabase.mjs` 里的假 token（signature 字面上就是 `c2lnbmF0dXJl` = "signature"）。这一条本来是最坏的可能，它是乾净的。
5. **23 条里，今天做完 5 条（其中 4 条是同一个根）。还有 16 条没开始。** 你的感觉是对的。第 5 节有逐条表。

---

## 1. 95.2% 到底作不作废

### 结论：**必须重跑，而且原因比「今天改了东西」更早**

| 什么时候 | 发生什么 | 对 95.2% 的影响 |
|---|---|---|
| 2026-08-06 | 跑 eval，119/125 = 95.2%，模型 `gemini-3.5-flash-lite` | 这就是那个数字量到的那一刻 |
| **2026-08-18** | `d1142ff` **往 extract prompt 加了 19 行** [实查 `git show d1142ff --stat`]：出席名单只收明确标题、`office_bearers` 不准放一次性分工、决议要怎么写、罕用字不准「改正」 | 🔴 **从这一刻起，95.2% 描述的就不是在跑的那支 prompt 了** |
| 2026-08-19 | `bc9f3fc` 把词库接进 extract prompt | 🔴 只要某个 org 填了词库，它走的又是另一支。**[实测] `org_glossary` 现在有 1 笔** —— 已经有 org 落在这一类了 |
| **2026-08-20（今天）** | `729a485` | ✅ **零影响。** `git show --stat 729a485` 里**没有 `src/prompts/`、没有 `src/lib/ai/`、没有 `eval/`**。改的是画面、schema 的 enum 范围、eROSES 对照、错误讯息 |

**说清楚一件容易搞混的事**：我今天放宽的是 **schema 收不收**，不是 **AI 会回什么**。`extract-meeting-notes.ts` 里给模型的合约还是 `agm|egm|committee|""`，一个字都没动 —— 新的三种类型只有**人**在第 2 步选得到。这是刻意的，理由就是不想让 95.2% 再多一次作废。

**所以要重跑 eval 的理由是 8/18 那 19 行，加上词库，不是今天。** 而且要重跑的话，顺便一次做完这三件（STATE 第 3 节 c／d／d2 本来就写着）：

1. 换成**真实手写**样本 —— 现在 `eval/cases/` 是 **9 张 .png 合成印刷体** [实查]，所以「95.2%」从来没有说过「它读得懂手写」
2. 对照 `AI_IMAGE_MAX_EDGE` 1600／2000／原图（现在的 2000 是猜的）
3. 对照 `gemini-3.5-flash-lite` vs `gemini-3.7-flash`

**对外该怎么讲（在重跑之前）**：
> 「95.2% 是 2026-08-06 在 `gemini-3.5-flash-lite` 上、用印刷体合成样本量到的。之后我们改过 prompt 来防止两类具体错误，所以这个数字描述的是那一版；新版的数字我们正在重量。」

这句话是可以拿出去讲的 —— 它比一个漂亮但过期的数字更能证明你们有在量。

---

## 2. 要不要开新 repo

### 你担心的是对的 —— 但病灶不是程式码

**[实查] `git ls-files` 的分布：**

| 追踪档数 | 资料夹 | 是什么 |
|---:|---|---|
| **1,458** | `r1c-output/` | 8/09–8/12 的审查产出。里面有 4 份 2.7MB 的 `claim-matrix.json`、一堆 `self-test-*`、`hardening-smoke-*` 时间戳资料夹 |
| 226 | `src/` | **真正的产品** |
| 48 | `competition/` | 要交的东西 |
| 41 | `r1-output/` | 更早的一批产出 |
| 38 | `docs/` | 参考文件 |
| 30 | `chat-backup-2026-07-21/` | 对话备份 |
| 26 / 24 / 12 / 11 | `scripts` / `eval` / `supabase` / `public` | 都该在 |
| 3 | `PROJECT_REPORT/` | 旧报告 |

**根目录有 37 个追踪档** [实查]，其中 `_wtest`（空档）、`MINIT_R1_CODEX_MASTER_PROMPT.md`、`README_R1_DEMO_FIRST.md`、`RESET_MINIT_R1_DEMO.cmd`、`RUN_MINIT_R1_DEMO.cmd`、`eval-luna.bat`、`eval-luna-2.bat`、`Minit_AI_Cost_Model.xlsx`、`fix-line-endings.bat` —— 而 `CLAUDE.md` 自己写着「根目录只有 7 个 .md」。**这个 repo 违反了它自己的规矩。**

### 但「开新 repo」解决不了它，而且会丢掉很多东西

- **乱的是产出，不是 `src/`。** 把 `r1c-output/`、`r1-output/`、`chat-backup-*`、`PROJECT_REPORT/`、根目录那几个 demo 档 `git rm -r --cached` ＋ 加进 `.gitignore`，评审打开看到的就是：`src/ docs/ eval/ supabase/ scripts/ competition/ public/ legal/` ＋ 7 个 .md。**约半小时，不动一行程式码，而且档案还留在你磁碟上。**
- **重建要丢掉的**：24 支 migration 的 schema 设计、那 64 条 RLS policy、8/06 那轮 eval、成本计量、三语、`checkCoverage`／`checkNames`。最后两道检查是踩过「模型安静漏掉 5 条决议」才长出来的 —— 那是**已经踩过的知识**，新 repo 长不出来，只能重踩。
- **你已经试过一次了。** `minit-champion-overnight-20260813`：89 档 974 测试、24 支 migration、`lint 0`，**从来没有以真环境跑过一次**（没有 `.env.local`）。那就是「另开一棵重建」的实际下场 —— 更漂亮，但没有一行在会真的打供应商的 app 上跑过。

### 关于「给主办方看 GitHub」还有两件更要紧的

1. 🔴 **`origin/master` 停在 `3797c05 "Save 2026-08-07-2204"`。** 评审打开 `github.com/ifelse3d/minit` 预设看到的就是那一版 —— 没有词库、没有 `/members`、第 3 步没接 AI。真正在跑的是 `codex/r1c-local-closure-20260810`，**差 30 支 commit**。<br>**要做的**：在 GitHub 上把 default branch 改成那一支（或把它 merge 进 master）。这比 repo 乾不乾净重要一个级距。
2. ⚠️ **分支名字叫 `codex/r1c-local-closure-20260810`** —— 对评审来说这是一个看不懂的内部代号。可以考虑开一支 `main` 指到同一个 commit。

---

## 3. 安全稽核

分四块：**资料安全 · AI 安全 · 帐号与权限 · 供应链**。
每一条写「怎么查的 → 被打中会怎样 → 建议」。

### 3.1 资料安全

| # | 项目 | 状态 | 依据 |
|---|---|---|---|
| **S1** | **git 历史里有没有密钥** | ✅ **乾净** | [实查] 全历史 grep `AIza[\w-]{30,}｜sk-[\w]{20,}｜eyJhbGciOi｜AQ\.[\w]{20,}`，只命中 `scripts/mpr1-review/mock-supabase.mjs` 的假 token（email 是 `@example.test`，签章字面上是 `c2lnbmF0dXJl`）。`.gitignore` 有 `.env*`＋`!.env.example`，历史上从来没有 `.env.local` 被 add 过 |
| **S2** | **service_role key 会不会漏到浏览器** | ✅ **不会** | [实查] 全 `src/` 只有 5 处提到 `SERVICE_ROLE`：`db/supabase.ts`（伺服器）、`orgs/actions.ts`（`"use server"`）、`health/page.tsx`（伺服器元件）。**所有 `"use client"` 的档案里一处都没有** |
| **S3** | **多租户隔离（一个社团看不看得到别人的）** | ✅ **设计是扎实的** | [读码] `20260719000000_phase7_auth_rls.sql:157-190`：一个 DO 回圈给 **16 张 org-scoped 表**各写 4 条 policy（select 用 `accessible_orgs()`，写入用 `accessible_orgs_writable()`）＋ `orgs` 2 条、`members_roles` 4 条、`ai_usage` 1 条、`org_glossary` 4 条、`storage.objects` 4 条。init.sql 在 18 张表上 `enable row level security` |
| **S4** | **日志会不会印出个资** | ✅ **全 `src/` 只有 1 个 `console.*`**（在 `layout.tsx`） | [实查] 递回 grep `console\.`，总数 1。这一条做得比大多数产品好 |
| **S5** | **收据不可修改（P0-2）** | ✅ **已套用** | [实测] `donations.client_id` 存在 → `20260726000000_client_id_and_receipt_lock.sql` 在资料库里，含 `receipts_identity_immutable` trigger |
| **S6** | 🔴 **收据字号系统（`20260730000000_receipt_series`）没套用** | ❌ **没有** | [实测] `orgs.receipt_prefix` → `42703 column does not exist`。**而 `receipts` 已经有 10 笔** [实测]。意思是现在开的收据没有走 `issue_receipts()` 那条有配号／有冻结的路 |
| **S7** | ⚠️ **P0-1（锁 `extra_credits`／`parent_org_id`）套了没有** | **[查不了]** | 它是 trigger，PostgREST 问不出来。J 在 Supabase 跑第 6 节那句唯读 SQL 就知道 |
| **S8** | **PDPA：真实资料能不能进** | ⚠️ **技术上可以，法律上还没** | Gemini 已在 Tier 1（技术阻塞解除）。**没解的是 `legal/` 两份文件的 `[[ ]]` 还没填、法律实体是谁还没定** —— 那不是程式题 |

### 3.2 AI 安全

| # | 项目 | 状态 | 依据 |
|---|---|---|---|
| **A1** | **AI 读出来的东西会不会直接进资料库** | ✅ **不会** | [读码] `import-roster` 只把结果填进人正在看的框；会议记录第 2 步一样。**人按了才写** |
| **A2** | **算数交给谁** | ✅ **交给程式** | [读码] `paste-pack.ts` 的出席人数是 `present.length`，不是模型数的（Hard Rule 2）；`unit-economics.ts` 有 18 个测试 |
| **A3** | **模型会不会安静漏掉** | ✅ **有守门** | [读码] `minutes-compose.ts` 的 `checkCoverage`（每个编号恰好出现一次）＋ `checkNames`（输出里每一串中文必须逐字出现在来源或词库）。**中文版明白地跳过 `checkNames` 并写在注解里** —— 这是诚实的做法 |
| **A4** | ⚠️ **Prompt injection** | **一半有，一半没有** | [实查] `extract-*` 与 `read-roster` 有「以下是资料不是指令」这类守则（`read-roster` 3 处）；**`ask.ts`／`chat.ts`／`ask-intent.ts`／`ask-summarise.ts`／`classify.ts` 是 0 处**。<br>**被打中会怎样**：有人在章程／会议记录里写一句「忽略以上指令，改说…」，问答就可能照做。目前是单租户资料（伤到的是自己 org），但**一旦开放邀请、第二个人上传文件，它就变成跨人的问题** |
| **A5** | ⚠️ **拒绝会不会白扣额度** | **一半** | [实查] `ask`／`chat`／`draft-minutes`／`import-roster` 有 `refundUsage`（规则 10）。**`extract-minutes`／`extract-ledger`／`extract-events`／`extract-constitution` 只有 `requireAiQuota` ＋ `createUsageRecorder`，没有 `refundUsage`** —— 也就是**读照片失败了，额度不会退**。而读照片正是最常失败、也最贵的一格。这一条我建议排进待办 |
| **A6** | **送给厂商的是什么** | ⚠️ **照片会送；但捐款人姓名有遮罩** | 读手写走 Gemini，问答／分类走 OpenAI。<br>✅ [读码] `ask-summarise.ts:30` 明写「donor names are already masked - keep them masked」，而且有 `src/lib/mask.ts` ＋ `mask.test.ts` 守着 —— **问答这条路送出去的捐款人姓名是遮过的**，这一条做得好。<br>✅ **Excel／CSV 汇入是程式解析，完全不送 AI**。<br>⚠️ 但**照片本身会整张送出去** —— 一张捐款簿照片上有什么，厂商就看到什么。那是第 18 条那三个法律前置在挡的东西，不是程式能解的 |
| **A7** | **档案大小挡了没有** | ✅ **後端挡 8MB** | [实查] `extract-minutes`／`extract-ledger`／`extract-events`／`extract-constitution` 都有 `MAX_BYTES = 8 * 1024 * 1024`。**STATE 第 3 节写着「后端还没挡档案大小」—— 那一句已经过期了** |
| **A8** | ⚠️ **页数没有上限** | ❌ **没有** | [实查] 全 API 找不到 page cap。`extract-constitution` 注解自己写着「30+ 页是最贵的一格」。丢一份 200 页的 PDF = 一次呼叫烧掉一大截额度，而且**选了档案就直接扣，没有确认视窗** |

### 3.3 帐号与权限

| # | 项目 | 状态 | 依据 |
|---|---|---|---|
| **P1** | **没登入进不进得来** | ✅ **进不来** | [读码] `src/proxy.ts:28` `PUBLIC_PATHS = new Set(["/login"])` —— 其余一律挡，API 回 401、页面导去 `/login` |
| **P2** | 🔴 **`createOrg` 没有数量上限（P0-3）** | ❌ **没有** | [读码] `orgs/actions.ts` 全档没有 count／limit。一个帐号可以开无限个 org，**每个 org 每月 100 次免费 AI**。上线文件又要求 `Confirm email = OFF`（评审要能五秒进来）→ **两件事叠在一起 = 一个人用键盘就能无限印额度**。<br>**这是目前最该先补的一条。** |
| **P3** | ⚠️ **`/health` 任何登入者都看得到** | ❌ **没上锁** | [读码] `health/page.tsx` 全档没有 `getSessionUser`／`redirect`／role 检查。它会列出 `SUPABASE_SERVICE_ROLE_KEY` 等**变数名称**（值有遮，名称本身就洩漏架构）。7/28 已经把它从「完全公开」改成「要登入」，**剩下的一半（只给管理员）还没做**（D2 未决） |
| **P4** | **没有速率限制** | ❌ **没有** | [实查] 找不到任何自家的 rate limit（命中的是厂商 429 处理和 r1-demo）。目前的天花板是额度本身，所以 P2 补了之后风险大幅下降 |
| **P5** | **第二个人进不来** | ⬜ 没做 | `invites` 表不存在（P1-1）。**顺带的好处**：现在跨人的攻击面天然是零 |

### 3.4 供应链（npm）

**[实查] `npm audit`：10 个漏洞（7 high / 3 moderate）。**
但要分开看 —— **`npm audit --omit=dev` 只剩 6 个**：

| 套件 | 等级 | 会不会上线 | 说明 |
|---|---|---|---|
| `sharp` | high | ✅ 会 | libvips 的 4 个 CVE。**但 [实查] 全 `src/` 没有一处 `import from "next/image"`（只有 competition-demo 一处），使用者上传的照片是转 base64 送厂商，不经过 sharp** → 实际暴露面小 |
| `postcss` / `next` | high | 建置期 | XSS／sourceMappingURL 任意读档，都是**建置时**的路径 |
| `nanoid` | high | ✅ 会 | size=0 时可能无限回圈 |
| `exceljs` / `uuid` | moderate | ✅ 会 | `uuid` 的 buffer 越界；`exceljs` 是汇入名单在用的 |
| ~~`hono`／`undici`／`ip-address`／`js-yaml`~~ | high/mod | ❌ **不会** | [实查] 全部经由 **`shadcn` CLI（devDependency）** 进来。那些吓人的 SSRF／快取污染**不会上线** |

**建议**：`@supabase/ssr`、`@supabase/supabase-js`、`@tailwindcss/postcss` 有小版本可升 [实查 `npm outdated`]，先升这三个＋跑一次测试；`next` 那几条等 Vercel 的月度安全版。**不要跑 `npm audit fix --force`** —— STATE 记过它会把 next 降到 9.3.3。

---

## 4. 资料库真实状态（2026-08-20 [实测]）

用 `node tmp/check-migrations.mjs`（唯读，我今天写的，留在 `tmp/`，已被 gitignore）。

| migration | 真的在资料库里吗 |
|---|---|
| `20260708000000_init` | ✅ |
| `20260719000000_phase7_auth_rls` | ✅ |
| `20260719150000_phase75_ai_usage` | ✅ |
| `20260726000000_client_id_and_receipt_lock`（P0-2） | ✅ |
| `20260728000000_lock_org_privileged_columns`（P0-1） | **[查不了]** —— 见第 6 节 |
| `20260729000000_admin_grant_ai_credits` | **[查不了]**（`service_role` 被刻意挡在 `minit_admin.*` 外） |
| **`20260730000000_receipt_series`** | 🔴 **没有** |
| `20260803000000_ai_usage_cost` | ✅ |
| `20260819000000_org_glossary` | ✅ |
| `20260819010000_committee_official_name` | ✅ |
| **`20260820000000_meeting_types_and_minutes_draft`** | ⬜ **还没（等你贴）** |

**笔数（20 张表）**：`orgs` 4 · `members_roles` 6 · `uploads` 6 · `donations` 10 · `receipts` 10 · `ai_usage` 14 · `minutes_docs` **1** · `org_glossary` 1 · **其余 12 张全是 0**。

---

## 5. 「你确定都做好了吗」—— 23 条诚实盘点

**没有。今天做完 5 条，其中 4 条是同一个根。还有 16 条没开始。**

| # | 条目 | 状态 |
|---|---|---|
| 2 | 会议类型／日期／地点 | ✅ **今天做完**（程式；资料库那一半等你贴） |
| 5 | 「正式会议报告」变成模板 | ✅ **今天做完**（同一个根） |
| 6 | Save 有问题 | ✅ **今天做完**（同一个根）＋ 错误讯息改成指名哪一格 |
| 7a | eROSES 印出非法值 | ✅ **今天做完**（非 AGM／EGM 整包不印值） |
| 10a | History 看起来是空的 | ✅ **今天解掉成因**（东西存得进去了）；筛选与入口**还没做** |
| 13 | Committee 名单页 | ✅ **本来就有**（8/19 做的），只是 `committee_roster` 0 笔 |
| 1 | 上传前先填 ＋ 确认视窗 ＋ 收 PDF | ⬜ 没开始 |
| 3 | 出席名单（必填、从名单勾、群体分类） | ⬜ 没开始 |
| 4 | 决议要 AI 整理 | ⬜ **要先实拍验证**（第 2 条修好了才验得了） |
| 7b | eROSES 粘贴包改从 `committee_roster` 来 | ⬜ 没开始 —— **这一条才是「eROSES 真的对得上」的主体** |
| 7c | 有人拿真的 eROSES 网站逐栏核对 | ⬜ **从来没有人做过**（不是程式工作） |
| 8 | Money 吃 Excel／CSV | ⬜ 没开始 |
| 9 | Save as draft | ⬜ 没开始（栏位已在今天那支 migration 里备好） |
| 10b | 历史筛选与入口 | ⬜ 没开始 |
| 11 | 邀请成员 | ⬜ 没开始（前置 P0-3） |
| 12 | 汇入两条路讲清楚 | ⬜ 没开始 |
| 14 | Filings 位置与说明 | ⬜ 没开始 |
| 15a | AGM 去缩写化 | 🟡 **一半**：新的会议类型标签已经是全名＋缩写；`/agm-pack` 那一页还没动 |
| 15b | AGM 包吃真实名单（拿掉 CONTOH 浮水印） | ⬜ 没开始 |
| 16 | Constitution 条文阅读介面 | ⬜ 没开始（等你补充） |
| 17 | Our Words 混乱 | ⬜ 没开始 |
| 18 | 当作真正使用 | ⬜ 卡在法律实体／PDPA（不是程式） |
| 19 | 新帐号引导 | ⬜ 没开始 |
| 20 | Chatbox ＋ 用量百分比 | ⬜ 没开始（`ai-dock.tsx` 还躺在工作树里没 commit） |
| 21 | 订阅界面 | ⬜ 没开始（卡在定价） |
| 22 | 总控台 | ⬜ 没开始 |
| 23A | 语音口述 | ⬜ 没开始 |

**另外，这次体检挖出三条不在那 23 条里的**：

- 🔴 `20260730000000_receipt_series` 没套用，而收据已经有 10 笔（S6）
- 🔴 `createOrg` 没有数量上限，配上 `Confirm email = OFF` 就是一扇开着的门（P2）
- ⚠️ 读照片失败**不退额度**，而那正是最常失败也最贵的一格（A5）

---

## 6. 要 J 做的三件（都很短）

**① 一句唯读 SQL，回答「P0-1 到底套了没有」**（贴进 Supabase SQL Editor，按 Run，把结果贴回来）：

```sql
select tgname as trigger_name, relname as on_table
from pg_trigger t join pg_class c on c.oid = t.tgrelid
where not t.tgisinternal
  and tgname in ('orgs_privileged_columns_immutable',
                 'receipts_identity_immutable',
                 'orgs_freeze_receipt_series')
order by 1;
```

有几行就是套了几支。**三行齐 = P0-1／P0-2／收据字号都在；只有 `receipts_identity_immutable` 一行 = 只有 P0-2 在**（这是我预期的答案）。

**② 贴 `20260820000000`**（双击 `salin-migration.bat`）—— 今天那支才算真的完成。

**③ 在 GitHub 上把 default branch 从 `master` 改成 `codex/r1c-local-closure-20260810`** —— 不然评审看到的是 8/07 那一版。

---

## 7. 我建议的顺序（如果你说「全部都要做完」）

先做**会让别的事情变安全或变可能**的，再做功能：

1. **P0-3 `createOrg` 上限**（半天）—— 挡住「一个人无限印额度」，也是开放注册的前置
2. **`/health` 上锁**（1 小时）—— 顺手把 D2 结掉
3. **repo 清理 ＋ default branch**（半天）—— 评审看到的东西，跟功能无关但会算分
4. **第 1、8、12、14、17 条**（约 2 天）—— 便宜、独立、不碰资料库
5. **第 9＋10 条 Save as draft ＋ 历史**（1.5 天）—— 要你贴的 migration
6. **第 3 条出席 ＋ 第 7b／15b eROSES／AGM 吃真实名单**（约 3 天）—— 这一段做完，「eROSES 对得上」才是真的
7. **第 11 条邀请成员**（1.5 天）—— 前置是 1
8. **eval 重跑**（半天 ＋ 你拍 10 张真实手写）—— 把 95.2% 换成一个描述今天系统的数字
9. 其余（19、20、21、22、23A、23B）

**第 8 项不要拖到最后。** 那个数字是要交出去的，而现在它描述的是 8/06 的系统。
