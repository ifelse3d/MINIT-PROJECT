// A-1 (work order 31): the root fallback — covers "/" and any segment
// without its own loading.tsx (health, legal), so no click is ever silent.
export { default } from "@/components/route-loading";
