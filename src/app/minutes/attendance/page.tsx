import { AttendanceReview } from "../attendance-review";

// Step 3 of the /minutes flow: the attendance list, on a page of its own.
export default function MinutesAttendancePage() {
  return <AttendanceReview />;
}
