import { MinutesDocument } from "../minutes-document";

// Steps 4-5 of the /minutes flow: the finished document, saving it, and the
// values to paste into eROSES.
export default function MinutesDocumentPage() {
  return <MinutesDocument />;
}
