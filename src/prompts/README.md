# /src/prompts

All LLM prompt templates live here as exported TypeScript strings with typed
params (CLAUDE.md Hard Rule 6: prompts are content, not code). First templates
arrive in Phase 1 (classification + extraction), then drafting, the eROSES
field mapping (`eroses-map.ts`), constitution Q&A, and announcements.
