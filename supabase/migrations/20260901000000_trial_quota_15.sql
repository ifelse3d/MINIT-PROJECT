-- ============================================================================
-- 20260901000000_trial_quota_15.sql
-- 試用額度：每月免費 AI 動作 100 → 15（J 2026-08-25 裁定，24 號施工單 建議③）。
--
-- 為什麼是 15：一次呼叫約 RM0.01–0.10（讀手寫最貴）。15 次/月 ≈ 每個試用 org
-- 每月成本上限 RM0.2–1.5 —— 夠試出價值（1 次章程＋1 場會議＋幾頁帳本＋幾個提問），
-- 刻意不夠長期白用。100 次等於把最貴那格開放到 RM10/org/月白送。
-- 「1 次=1%」的舊疑問不受影響：畫面上的百分比是 用量/額度 算出來的，
-- 額度改了百分比自動跟著對；每次動作的「約佔 X%」承諾已按 #14 全部拿掉。
--
-- ⚠️ 這支只改「預設值」：影響**之後新建**的 org。
--    已存在的 org 要不要一起降到 15，由 J 決定 —— 第 2 段（預設是註解、不會跑）。
-- ============================================================================

-- ── 第 1 段（會執行）：只改預設 ────────────────────────────────────────────
-- 新開的 org 免費額度一律 15。已存在的 org 完全不動。
alter table public.orgs
  alter column monthly_free_quota set default 15;

-- ── 第 2 段（預設不執行）：連舊 org 一起改 ────────────────────────────────
-- 要跑的話，把下面整段的註解拿掉再 RUN 一次。
-- 只降還在吃「舊預設 100」的 org；被 minit_admin.grant_ai_credits 特別調過的
-- （不等於 100 的）一律不碰 —— 那些數字是人特地給的。
--
-- update public.orgs
--    set monthly_free_quota = 15
--  where monthly_free_quota = 100;

-- ── 驗證（唯讀，跑不跑都行）────────────────────────────────────────────────
-- 預設值應該顯示 15：
--   select column_default
--     from information_schema.columns
--    where table_schema = 'public' and table_name = 'orgs'
--      and column_name = 'monthly_free_quota';
-- 每個 org 現在的額度：
--   select id, name, monthly_free_quota, extra_credits from public.orgs;
